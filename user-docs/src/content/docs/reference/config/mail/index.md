---
title: "mail"
description: "the relay, the sender, and the local template overrides"
---

<!-- Generated by genconfdocs from the config structs in server/config. Do not edit. -->

Disabled by default; see docs/operations/email-notifications.md.  Disabled is the default. A server that has never been told about a relay should not try to reach one, and notifications are an addition to the certificate flows rather than a part of them — every path that emits one works identically when this is off.

| Key | Type | Default |
| --- | --- | --- |
| [`mail.enabled`](#enabled) | bool | `false` |
| [`mail.from`](#from) | string | `empty` |
| [`mail.reply_to`](#reply_to) | string | `empty` |
| [`mail.subject_prefix`](#subject_prefix) | string | `empty` |
| [`mail.template_dir`](#template_dir) | string | `empty` |
| [`mail.smtp.host`](/ssoossh/reference/config/mail/smtp/#host) | string | `localhost` |
| [`mail.smtp.port`](/ssoossh/reference/config/mail/smtp/#port) | int | `25` |
| [`mail.smtp.tls`](/ssoossh/reference/config/mail/smtp/#tls) | string | `opportunistic` |
| [`mail.smtp.server_name`](/ssoossh/reference/config/mail/smtp/#server_name) | string | `empty` |
| [`mail.smtp.ca_file`](/ssoossh/reference/config/mail/smtp/#ca_file) | string | `empty` |
| [`mail.smtp.insecure_skip_verify`](/ssoossh/reference/config/mail/smtp/#insecure_skip_verify) | bool | `false` |
| [`mail.smtp.auth`](/ssoossh/reference/config/mail/smtp/#auth) | string | `none` |
| [`mail.smtp.username`](/ssoossh/reference/config/mail/smtp/#username) | string | `empty` |
| [`mail.smtp.password`](/ssoossh/reference/config/mail/smtp/#password) | string | `empty` |
| [`mail.smtp.password_file`](/ssoossh/reference/config/mail/smtp/#password_file) | string | `empty` |
| [`mail.smtp.helo`](/ssoossh/reference/config/mail/smtp/#helo) | string | `empty` |
| [`mail.smtp.timeout`](/ssoossh/reference/config/mail/smtp/#timeout) | duration | `15s` |
| [`mail.logging.*`](/ssoossh/reference/config/mail/logging/#maillogging) | | |
| [`mail.logging.level`](/ssoossh/reference/config/mail/logging/#level) | string | `info` |
| [`mail.logging.add_source`](/ssoossh/reference/config/mail/logging/#add_source) | bool | `false` |
| [`mail.logging.log_json`](/ssoossh/reference/config/mail/logging/#log_json) | bool | `false` |

## `enabled`

`bool`, default `false`

Turns outbound mail on. With it false the notification handler is not registered at all, so no event is queued and nothing is rendered.

## `from`

`string`, default `empty`

The envelope and header sender, e.g. "ssoossh@example.com" or "ssoossh &lt;noreply@example.com>". Required when Enabled.

## `reply_to`

`string`, default `empty`

An optional Reply-To header. Worth setting to a monitored address: these messages report credential activity, and the first thing a recipient does with an unexpected one is try to reply.

## `subject_prefix`

`string`, default `empty`

Prepended to every rendered subject, e.g. "[ssoossh] ". Empty adds nothing. Templates can also write their own subject in full; this exists so an operator can tag every message without editing any template.

## `template_dir`

`string`, default `empty`

An optional directory of local template overrides. A file there replaces the embedded template of the same name; anything absent falls back to the embedded one, so overriding a single message does not mean vendoring the whole set. See docs/operations/email-notifications.md.
