# ssoossh documentation

**ssoossh** (pronounced *sue-sssh*) is SSO for SSH: users authenticate via OIDC
and receive short-lived SSH certificates instead of managing long-lived keys.
Self-hosted, homelab-friendly, early development.

## Start here

| Document | What it covers |
| --- | --- |
| [getting-started.md](getting-started.md) | The shortest path to a working `ssh login` |
| [what-ssoossh-is.md](what-ssoossh-is.md) | The problem, how it works, components, certificate types and terms |
| [features.md](features.md) | Everything implemented, one section per feature, with the invariants each is built around |
| [flows.md](flows.md) | Mermaid sequence diagrams for each flow |

## Operating it

| Document | What it covers |
| --- | --- |
| [deployment.md](deployment.md) | Operator runbook: CA key, systemd, `sshd` trust, OIDC provider, reverse proxy, multi-instance, PAM |
| [ssoosshd.yaml.default](ssoosshd.yaml.default) | Annotated server configuration sample |
| [ssoossh.yaml.default](ssoossh.yaml.default) | Annotated client configuration sample |
| [client-settings-enforcement.md](client-settings-enforcement.md) | Locking down client settings: `enforce` file, Windows Group Policy, macOS managed preferences, precedence |
| [certificate-lifetime-policy.md](certificate-lifetime-policy.md) | Lifetime and options derived from issuance context: group tiers, source rules, the footguns |
| [pam.d-sudo.example](pam.d-sudo.example) | Annotated `pam.d` stack for `sudo`/`su` |
| [man/](man/) | Man pages (`.1` client, `.5` config formats, `.8` server) |
| [DEPENDENCY-SCANNING.md](DEPENDENCY-SCANNING.md) | How dependencies are scanned and kept current |
| [release-notes.md](release-notes.md) | Draft notes for the first tagged release |

## How it works inside

| Document | What it covers |
| --- | --- |
| [signing-pipeline.md](signing-pipeline.md) | The create → approve → sign → deliver pipeline: components, topics, and the decisions that constrain the code |
| [ssoossh-context.md](ssoossh-context.md) | Design context: hard constraints, invocation modes, enrollment, CLI surface, open questions |
| [openapi.yaml](openapi.yaml) | The HTTP API wire contract. Generated by `make openapi` — edit the handler annotations, not this file |
| [wire-types.md](wire-types.md) | How the server, Go client, and web UI are kept from drifting: shared types, tygo generation, the golden test |
| [e2e-testing-plan.md](e2e-testing-plan.md) | Running the whole loop — login, browser approval, certificate, `ssh` — on every pull request |

## Decisions

| Document | What it covers |
| --- | --- |
| [decisions.md](decisions.md) | The consolidated record of what was deferred, declined, or settled, and why. **Read before proposing work** |

## In progress

| Document | What it covers |
| --- | --- |
| [multi-instance-safety-plan.md](multi-instance-safety-plan.md) | Running more than one `ssoosshd` against a shared database: what is done, what remains |
| [signer-split-deferred.md](signer-split-deferred.md) | Running the signer as its own process: NATS, mTLS authorization, startup modes |

## Development

Working notes for people hacking on ssoossh itself, in [dev/](dev/): the
work queues ([changes-now](dev/changes-now.md),
[changes-next](dev/changes-next.md)), the
[database schema audit](dev/database-schema-audit-2026-08-22.md), the
[mutation-testing findings](dev/mutation-testing-findings.md), and the
[parallel agent workflow](dev/parallel-agent-workflow.md) used to develop
several features at once.
