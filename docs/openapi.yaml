components:
  schemas:
    apitypes.ApproveResponse:
      properties:
        status:
          description: |-
            Status is always "signing" today — included so the response shape
            can carry more without a breaking change later.
          type: string
      required:
      - status
      type: object
    apitypes.CAResponse:
      properties:
        ca:
          type: string
      required:
      - ca
      type: object
    apitypes.ConsoleRequestBody:
      properties:
        caller_gid:
          type: integer
        caller_pid:
          type: integer
        caller_ppid:
          type: integer
        caller_uid:
          type: integer
        client:
          type: string
        client_time:
          type: string
        hostname:
          description: |-
            Same fields, same trust, as PAMRequestBody's — and they matter more
            here, because a console certificate authorizes a whole session.
          type: string
        machine_id:
          type: string
        mode:
          type: string
        os:
          type: string
        pam_service:
          type: string
        process:
          type: string
        public_key:
          type: string
        remote_host:
          type: string
        requested_options:
          $ref: '#/components/schemas/apitypes.RequestedOptions'
        requesting_user:
          description: The rest of the host context; see PAMRequestBody for each field.
          type: string
        trusted_ca_fingerprints:
          items:
            type: string
          type: array
          uniqueItems: false
        tty:
          type: string
        username:
          type: string
      required:
      - public_key
      - username
      type: object
    apitypes.CreateRequestResponse:
      properties:
        approval_url:
          type: string
        events_url:
          type: string
        expires_at:
          description: |-
            ExpiresAt is when this request stops being approvable, from its own
            type's budget (cert_options.<type>.client_timeout, falling back to
            cert_options.client_timeout). Populated for every type.

            A client bounds its wait by this rather than by a local guess, and a
            console displays the time remaining from it. Without it, the client's
            timeout and the server's budget are two numbers an operator keeps in
            agreement by hand, and disagreement surfaces as a client still
            waiting on a request the server already killed. RFC 8628 carries
            expires_in for the same reason.
          type: string
        request_id:
          type: string
        user_code:
          description: |-
            UserCode is the short code a human types into the web UI, grouped
            for display ("K7M4-QP2X"). Console requests only; empty for every
            other type, so no existing consumer changes.

            This is the only response it ever appears in. It is not in the SSE
            stream, not in the request detail, and not in any audit record.
          type: string
        verification_url:
          description: |-
            VerificationURL is the page that accepts UserCode;
            VerificationURLComplete embeds the code so a device with a camera or
            a keyboard can skip the code box entirely. Relative, like the two
            URLs above, and console requests only.

            Complete is kept short (/c/<code>, not /approve/<uuid>) because it is
            what a QR code drawn in an 80x24 terminal has to encode.
          type: string
        verification_url_complete:
          type: string
      required:
      - approval_url
      - events_url
      - request_id
      type: object
    apitypes.DenyResponse:
      properties:
        status:
          type: string
      required:
      - status
      type: object
    apitypes.PAMRequestBody:
      properties:
        caller_gid:
          type: integer
        caller_pid:
          type: integer
        caller_ppid:
          type: integer
        caller_uid:
          description: |-
            CallerUID, CallerPID and CallerPPID identify the process on the host,
            for joining with the host's own logs. Pointers so an absent value is
            distinguishable from uid 0 or pid 0.
            CallerGID is the process's group, added with the Go client and
            accepted here so one column and one audit key mean the same thing
            whichever implementation sent the request; the C module does not
            report it yet.
          type: integer
        client:
          description: |-
            Client names the module and its version ("pam_ssoossh-c/0.3.0"),
            which is what tells the two module implementations apart in a log.
          type: string
        client_time:
          description: |-
            ClientTime is the host's own clock when it built the request, so
            skew against the server is visible before it fails a login.
          type: string
        hostname:
          description: |-
            The context an approver needs to tell a request they caused from one
            they did not: which machine, through which PAM service, at which
            terminal, and — since a real console has no remote host — whether
            PAM_RHOST says this did not come from a console at all.

            Every one of these is self-reported by an unauthenticated caller and
            is displayed as a claim, never as a fact. Optional, so a client that
            does not read PAM items still works.
          type: string
        machine_id:
          description: |-
            MachineID is a stable per-install identifier (/etc/machine-id or
            kern.hostuuid), so a host is still recognisable after a rename and
            two hosts claiming one name are distinguishable.
          type: string
        mode:
          description: |-
            Mode is the module's configured mode argument as written in pam.d
            ("auto", "sudo", "console"), not the route it resolved to; the
            endpoint already says that. Together they explain why a request
            arrived as a console one.
          type: string
        os:
          description: |-
            OS is the platform as the host describes itself: os-release
            PRETTY_NAME followed by uname -s and -r.
          type: string
        pam_service:
          type: string
        process:
          description: |-
            Process is the PAM host process's command line ("sudo -i",
            "sudo systemctl restart nginx"), read from /proc/self/cmdline where
            the platform has it. Empty where it does not.
          type: string
        public_key:
          type: string
        remote_host:
          type: string
        requested_options:
          $ref: '#/components/schemas/apitypes.RequestedOptions'
        requesting_user:
          description: |-
            The rest of the host context, same trust as the four above: every value is
            what the module read off its own process and machine, and the approval
            page renders each as a claim. They exist so an approver of a `sudo` can
            see which command is asking, on which machine, invoked by whom, and so the
            audit line joins against the host's own auditd or journal. See
            https://mnestor.github.io/ssoossh/internals/host-context/.

            RequestingUser is PAM_RUSER: who invoked the service, as opposed to
            Username, the account being authenticated. Under `su` or sudo's
            targetpw they differ.
          type: string
        trusted_ca_fingerprints:
          description: |-
            TrustedCAFingerprints are the SHA256 fingerprints of the keys in the
            module's trusted-ca-file, in OpenSSH form. The module will reject a
            certificate signed by any other key, so the server can warn the
            approver before that happens rather than after.
          items:
            type: string
          type: array
          uniqueItems: false
        tty:
          type: string
        username:
          type: string
      required:
      - public_key
      - username
      type: object
    apitypes.RequestedOptions:
      properties:
        extensions:
          description: |-
            Extensions are the SSH certificate extensions requested (e.g.
            "permit-pty", "permit-agent-forwarding").
          items:
            type: string
          type: array
          uniqueItems: false
        force_command:
          description: ForceCommand is the SSH "force-command" critical option request.
          type: string
        no_touch_required:
          description: |-
            NoTouchRequired requests the OpenSSH "no-touch-required" extension.
            Only meaningful for service enrollment of a hardware-backed sk- key.
          type: boolean
        source_addresses:
          description: |-
            SourceAddresses are the caller's own interface addresses, unioned
            server-side with the address the request was observed from.
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    apitypes.RetrieveRequestBody:
      properties:
        code:
          type: string
      required:
      - code
      type: object
    apitypes.RetrieveResponse:
      properties:
        certificate:
          type: string
      required:
      - certificate
      type: object
    apitypes.ServiceEnrollRequestBody:
      properties:
        public_key:
          type: string
        requested_options:
          $ref: '#/components/schemas/apitypes.RequestedOptions'
      required:
      - public_key
      type: object
    apitypes.UserRequestBody:
      properties:
        caller_gid:
          type: integer
        caller_pid:
          type: integer
        caller_ppid:
          type: integer
        caller_uid:
          description: |-
            CallerUID, CallerGID, CallerPID and CallerPPID identify the client
            process, for joining with the machine's own logs. Pointers so an
            absent value is distinguishable from uid 0 or gid 0; the two ids are
            absent on Windows, which has neither.
          type: integer
        client:
          description: |-
            Client names the implementation and version ("ssoossh/1.2.3"), which
            is what tells this client and pam_ssoossh apart in a log.
          type: string
        client_time:
          description: |-
            ClientTime is the machine's own clock when it built the request, so
            skew against the server is visible before it breaks a login.
          type: string
        local_hostname:
          type: string
        local_username:
          type: string
        machine_id:
          description: |-
            MachineID is a stable per-install identifier: /etc/machine-id on
            Linux, kern.uuid on macOS, kern.hostuuid on FreeBSD, MachineGuid on
            Windows.
          type: string
        os:
          description: OS is the platform as the machine describes itself.
          type: string
        process:
          description: |-
            Process is the client's own argv ("ssoossh ssh login --force"),
            the analogue of the PAM host process's command line. It is what
            distinguishes an interactive login from a ProxyCommand invocation.
          type: string
        public_key:
          type: string
        remote_host:
          description: |-
            RemoteHost is the peer address when the client is itself running
            inside an SSH session (SSH_CONNECTION), the analogue of PAM_RHOST.
            Empty at a local terminal.
          type: string
        requested_options:
          $ref: '#/components/schemas/apitypes.RequestedOptions'
        requesting_user:
          description: |-
            The rest of the host context, the same set and the same trust as
            PAMRequestBody's -- see that type for what each field means and
            https://mnestor.github.io/ssoossh/internals/host-context/ for how far
            each one travels. The client reads them off its own process and
            machine (internal/hostinfo); every value is self-reported by an
            unauthenticated caller and the approval page renders each as a claim.

            A user request carried only the two identity fields above until this
            existed, so an approver deciding a `ssh login` saw two strings where
            an approver deciding a `sudo` saw a dozen. All optional: an older
            client that sends none of them still works, and a field the platform
            cannot answer is simply absent.

            PAMService and Mode have no analogue here and are not sent. The
            endpoint already says this is a user request, and there is no pam.d
            line to have configured a mode in.

            RequestingUser is SUDO_USER, the analogue of PAM_RUSER: who invoked
            the client, when that differs from the account it runs as.
          type: string
        trusted_ca_fingerprints:
          description: |-
            TrustedCAFingerprints is the SHA256 fingerprint of the CA public key
            this client has *pinned* (the `capubkey` setting), in OpenSSH form.
            Sent only when it was pinned: a key the client fetched from this same
            server says nothing the server does not already know, and reporting
            one as trust would be circular.
          items:
            type: string
          type: array
          uniqueItems: false
        tty:
          description: TTY is the controlling terminal where the platform can name
            one.
          type: string
      required:
      - public_key
      type: object
    gin.H:
      additionalProperties: {}
      type: object
    model.CertificateRequestStatus:
      enum:
      - pending
      - signing
      - approved
      - enrolled
      - denied
      - expired
      - failed
      type: string
      x-enum-varnames:
      - CertificateRequestStatusPending
      - CertificateRequestStatusSigning
      - CertificateRequestStatusApproved
      - CertificateRequestStatusEnrolled
      - CertificateRequestStatusDenied
      - CertificateRequestStatusExpired
      - CertificateRequestStatusFailed
    model.CertificateType:
      enum:
      - user
      - service
      - pam
      - console
      type: string
      x-enum-varnames:
      - CertificateTypeUser
      - CertificateTypeService
      - CertificateTypePAM
      - CertificateTypeConsole
    openapidoc.ApproveEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/apitypes.ApproveResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.BrandingEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.BrandingResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CAEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/apitypes.CAResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CertificateDetailEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.CertificateResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CertificateListAdminEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.CertificateListAdminResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CertificateListEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.CertificateListResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CreateRequestEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/apitypes.CreateRequestResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.CurrentUserEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.CurrentUserResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.DenyEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/apitypes.DenyResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.EnrollmentRetrievalsEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.EnrollmentRetrievalsResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.ErrorEnvelope:
      properties:
        data:
          description: Data is always null on an error response.
        error:
          description: |-
            Error is a human-readable message. Branch on the HTTP status rather
            than this string; it is for humans and logs.
          example: certificate request "9f1c…" not found
          type: string
        error_code:
          description: |-
            ErrorCode is a machine-readable error classifier. One of the error_code
            constants from internal/apitypes; use this to decide whether to retry
            or branch on the failure mode.
          example: not_found
          type: string
      required:
      - error
      - error_code
      type: object
    openapidoc.HealthPayload:
      properties:
        status:
          example: ok
          type: string
      required:
      - status
      type: object
    openapidoc.LogoutEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/openapidoc.LogoutPayload'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.LogoutPayload:
      properties:
        logged_out:
          type: boolean
      required:
      - logged_out
      type: object
    openapidoc.NotificationPreferencesEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.NotificationPreferencesResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.RequestDetailEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.RequestDetailResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.ResolveCodeEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.ResolveCodeResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.RetrieveEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/apitypes.RetrieveResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.ServiceEnrollmentsEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.ServiceEnrollmentsResponse'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.SetNotificationEmailEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.SetNotificationEmailRequestBody'
        error:
          type: string
      required:
      - data
      type: object
    openapidoc.VersionEnvelope:
      properties:
        data:
          $ref: '#/components/schemas/webtypes.VersionResponse'
        error:
          type: string
      required:
      - data
      type: object
    webtypes.AdminEnrollmentResponse:
      properties:
        approved_by_email:
          type: string
        approved_by_username:
          description: |-
            ApprovedByUsername and ApprovedByEmail name the user who approved this
            enrollment. Provenance, not ownership — the code outlives their access
            to it and is not theirs to move.
          type: string
        certificate_valid_seconds:
          description: CertificateValidSeconds is how long each redeemed certificate
            is valid.
          type: integer
        created_at:
          description: CreatedAt is when the enrollment was approved.
          type: string
        expires_at:
          description: ExpiresAt bounds the code.
          type: string
        first_redeemed_at:
          description: |-
            FirstRedeemedAt is the first successful redemption, absent for a code
            that has never produced a certificate.
          type: string
        id:
          type: string
        key_id:
          description: |-
            KeyID is likewise fixed at approval and lands verbatim in every
            certificate.
          type: string
        last_retrieved_at:
          description: LastRetrievedAt is the most recent redemption attempt.
          type: string
        notification_email:
          description: |-
            NotificationEmail is where notifications about this enrollment go
            instead of to every holder of the service account. Empty means they
            fan out. Visible to auditors because it decides who hears about a
            credential; changing it needs SOC.
          type: string
        options:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        principals:
          description: Principals is what every certificate this code produces carries.
          items:
            type: string
          type: array
          uniqueItems: false
        public_key_fingerprint:
          description: |-
            PublicKeyFingerprint identifies the keypair the code is bound to.
            Empty if the stored key could not be parsed.
          type: string
        retrieval_count:
          description: RetrievalCount counts every logged redemption attempt.
          type: integer
        service_account:
          description: |-
            ServiceAccount is the account this code was approved for, and who owns
            it: everyone holding the account (see
            docs/proposals/enrollment-group-ownership.md).
          type: string
      required:
      - approved_by_email
      - approved_by_username
      - created_at
      - expires_at
      - id
      - key_id
      - options
      - principals
      - retrieval_count
      - service_account
      type: object
    webtypes.AdminEnrollmentsResponse:
      properties:
        enrollments:
          items:
            $ref: '#/components/schemas/webtypes.AdminEnrollmentResponse'
          type: array
          uniqueItems: false
        meta:
          $ref: '#/components/schemas/webtypes.PageMeta'
      required:
      - enrollments
      - meta
      type: object
    webtypes.AdminUserDetail:
      properties:
        certificate_count:
          description: CertificateCount is how many certificates have been issued
            to this user.
          type: integer
        created_at:
          description: CreatedAt is when the user first authenticated.
          type: string
        directory:
          $ref: '#/components/schemas/webtypes.AdminUserDirectory'
        directory_enabled:
          description: |-
            DirectoryEnabled mirrors ldap.enabled, and is what disambiguates an
            absent Directory. Without it, "no directory record" means either "this
            person has never been enriched" or "the directory is switched off and
            their record is being withheld" — two states that call for opposite
            actions from whoever is reading the page.
          type: boolean
        directory_overrides:
          description: |-
            DirectoryOverrides names each identity field the directory currently
            supplies in place of the OIDC value, with both sides shown. Empty
            when LDAP is disabled, unconfigured, or has never resolved this
            person — in which case the OIDC values above are what the server
            acts on.
          items:
            $ref: '#/components/schemas/webtypes.AdminUserOverride'
          type: array
          uniqueItems: false
        disabled_at:
          description: DisabledAt is when an admin disabled this user. Omitted if
            not disabled.
          type: string
        disabled_by_user_id:
          description: |-
            DisabledByUserID and DisabledByUsername identify the admin that
            disabled this user. Both omitted if not disabled.
          type: string
        disabled_by_username:
          type: string
        disabled_reason:
          description: |-
            DisabledReason is why the account was disabled, required at the API
            and shown here because this is where it meets the person deciding
            whether to re-enable. Omitted if not disabled.
          type: string
        disabled_source:
          description: |-
            DisabledSource says what disabled the account: "admin", "soc" or
            "ldap_sync". It is what makes auto-re-enable safe — the sync clears
            only its own disables — so it belongs next to the reason rather than
            only in the database. Omitted if not disabled, and absent on rows
            predating the column.
          type: string
        email:
          description: |-
            Email is the user's email from OIDC, possibly empty. Not an
            identifier either, for the same reason as Username.
          type: string
        extra_fields:
          additionalProperties: {}
          description: ExtraFields are operator-configured extra claims, decoded from
            stored JSON map.
          type: object
        groups:
          description: |-
            Groups are the persisted group memberships. Both capture paths while
            ldap.enabled is true; OIDC only when it is false, on the same
            reasoning as Directory below — the directory rows are frozen, so
            they are neither shown nor used for notification fan-out until the
            directory is switched back on.
            Never an authorization input (see
            https://mnestor.github.io/ssoossh/internals/invariants/): this is
            what the server recorded, shown so an operator can see why a
            notification reached someone, or why a group they expected is
            missing.
          items:
            $ref: '#/components/schemas/webtypes.AdminUserGroup'
          type: array
          uniqueItems: false
        id:
          description: ID is the stable user identifier.
          type: string
        name:
          description: |-
            Name is the person's human-readable name. Display only: it is not a
            principal, not a key ID input, and never an authorization input.
            Empty when neither the identity provider nor the directory supplied
            one.
          type: string
        notification_preferences:
          description: |-
            NotificationPreferences are the explicit choices this user has made.
            A kind with no row here is on its registered default, so an empty
            list means "everything default" rather than "everything off".
          items:
            $ref: '#/components/schemas/webtypes.AdminUserNotificationPreference'
          type: array
          uniqueItems: false
        other_accounts:
          description: |-
            OtherAccounts, ServiceAccounts, ExtraFields and Name above are the
            OIDC capture — exactly what the ID token carried at the last login,
            as stored on the users row. They are deliberately *not* the values
            the server acts on: a configured ldap.fields entry overrides its
            OIDC counterpart wholesale, and DirectoryOverrides below names every
            field where that is currently happening and what the effective value
            is.

            Showing the OIDC half even when it is overridden is the point. An
            operator debugging "why is this person missing a principal" needs to
            see both sides and which one won; showing only the winner makes a
            misconfigured claim mapping invisible.
          items:
            type: string
          type: array
          uniqueItems: false
        service_accounts:
          description: ServiceAccounts are service accounts from OIDC, decoded from
            stored JSON.
          items:
            type: string
          type: array
          uniqueItems: false
        service_enrollment_count:
          description: |-
            ServiceEnrollmentCount is how many live (not expired) service
            enrollments this user approved. Provenance, not a consequence of
            disabling them: the codes belong to their service accounts and keep
            working (see docs/proposals/enrollment-group-ownership.md).
          type: integer
        subject:
          description: |-
            Subject is the unique account identifier, read from the claim named
            by authentication.fields.subject ("sub" by default). It is the only
            value a login is keyed by, and the only one guaranteed not to move.
          type: string
        updated_at:
          description: UpdatedAt is when the user's identity was last refreshed at
            login.
          type: string
        username:
          description: |-
            Username is the OIDC claim username. Not an identifier — it changes
            when a person is renamed, which is what Subject exists to survive.
          type: string
      required:
      - certificate_count
      - created_at
      - directory_overrides
      - email
      - extra_fields
      - groups
      - id
      - name
      - notification_preferences
      - other_accounts
      - service_accounts
      - service_enrollment_count
      - subject
      - updated_at
      - username
      type: object
    webtypes.AdminUserDirectory:
      description: |-
        Directory is the user's directory bookkeeping row, absent when they
        have never been enriched. It is what answers "why is this person
        missing a group" from data already stored.

        Also absent whenever ldap.enabled is false, even for a user who has a
        row. Switching the directory off stops the sync, so everything in
        that row is frozen at whatever the last pass read, and the server no
        longer acts on any of it — the session identity falls back to the
        OIDC values on the next request. Presenting frozen data beside live
        data with no way to tell them apart is how someone ends up granting
        access on a principal list that has not been true for months.
      properties:
        attributes:
          additionalProperties:
            items:
              type: string
            type: array
          description: |-
            Attributes is the stored field map — the same values the login path
            merges onto the identity, and the last-known-good cache it falls back
            to when the directory is unreachable.
          type: object
        consecutive_misses:
          description: |-
            ConsecutiveMisses is how many passes have observed the absence.
            Reporting only: the disable is decided on elapsed time, since a pass
            count measures replica count rather than absence.
          type: integer
        directory_id:
          description: |-
            DirectoryID is the entry's unique, immutable identifier (the
            attribute named by ldap.id_attribute), and the anchor that survives a
            rename or a move between OUs. Empty when ldap.id_attribute is
            unconfigured, which leaves resolution on the DN-then-filter path.
          type: string
        dn:
          description: DN is the entry's distinguished name from the last successful
            read.
          type: string
        first_missing_at:
          description: |-
            FirstMissingAt is when the entry was first found to be missing, and
            what the auto-disable threshold is measured against. Absent means the
            entry is currently resolving.
          type: string
        last_seen_at:
          description: |-
            LastSeenAt is the last successful read of the entry; LastSyncedAt is
            the last pass that reached the directory at all. They differ exactly
            when the entry is missing.
          type: string
        last_synced_at:
          type: string
      required:
      - attributes
      - directory_id
      - dn
      type: object
    webtypes.AdminUserGroup:
      properties:
        first_seen_at:
          description: |-
            FirstSeenAt survives a refresh that keeps the membership, so "since
            when" is answerable; LastSeenAt is the last capture that saw it.
          type: string
        last_seen_at:
          type: string
        name:
          description: |-
            Name is the reduced, comparable name: a memberOf DN is reduced to its
            CN before it is stored.
          type: string
        source:
          description: |-
            Source is "oidc" or "ldap". The two capture paths never collide and
            a name can appear under both.
          type: string
      required:
      - first_seen_at
      - last_seen_at
      - name
      - source
      type: object
    webtypes.AdminUserNotificationPreference:
      properties:
        enabled:
          type: boolean
        kind:
          type: string
        updated_at:
          description: UpdatedAt is when the choice was last changed.
          type: string
      required:
      - kind
      - updated_at
      type: object
    webtypes.AdminUserOverride:
      properties:
        effective:
          description: |-
            Effective is what the directory supplies, and therefore what the
            server actually acts on for this field.
          items:
            type: string
          type: array
          uniqueItems: false
        field:
          description: |-
            Field is the destination name: "other_accounts", "service_accounts",
            "name", or an operator-chosen extra field.
          type: string
        oidc:
          description: |-
            OIDC is what the ID token supplied for this field at the last login,
            shown even though it lost — a claim mapping that is quietly wrong is
            invisible otherwise. Empty when the token carried nothing.
          items:
            type: string
          type: array
          uniqueItems: false
      required:
      - effective
      - field
      - oidc
      type: object
    webtypes.AdminUserSummary:
      properties:
        created_at:
          description: CreatedAt is when the user first authenticated.
          type: string
        disabled_at:
          description: |-
            DisabledAt is when an admin disabled this user. Omitted (null) if not
            disabled.
          type: string
        disabled_by_username:
          description: |-
            DisabledByUsername is the username of the admin that disabled this user.
            Only populated when DisabledAt is non-null.
          type: string
        email:
          description: Email is the user's email from OIDC, possibly empty or changed
            at login.
          type: string
        id:
          description: ID is the stable user identifier.
          type: string
        name:
          description: |-
            Name is the person's human-readable name, so a list of usernames can
            be scanned by someone who thinks in names. Display only, and empty
            when nothing supplied one.
          type: string
        subject:
          description: |-
            Subject is the unique account identifier (authentication.fields.subject,
            "sub" by default) — the one field here that is stable across logins.
          type: string
        updated_at:
          description: UpdatedAt is when the user's identity was last refreshed at
            login.
          type: string
        username:
          description: Username is the OIDC claim username, possibly changed at each
            login.
          type: string
      required:
      - created_at
      - email
      - id
      - name
      - subject
      - updated_at
      - username
      type: object
    webtypes.AdminUsersListResponse:
      properties:
        meta:
          $ref: '#/components/schemas/webtypes.PageMeta'
        users:
          items:
            $ref: '#/components/schemas/webtypes.AdminUserSummary'
          type: array
          uniqueItems: false
      required:
      - meta
      - users
      type: object
    webtypes.ApproveRequestBody:
      properties:
        notification_email:
          description: |-
            NotificationEmail optionally points every notification about the
            resulting enrollment at one address instead of fanning out to every
            holder of the service account. Service-type requests only; ignored
            for others. Empty means fan out, which is the default.

            Approval is where this belongs because it is the moment the approver
            is already deciding what the enrollment is for: a team alias entered
            here reaches the people who run the job rather than the one person
            who clicked approve. It stays editable afterwards.
          type: string
        principals:
          items:
            type: string
          type: array
          uniqueItems: false
        service_account:
          type: string
      type: object
    webtypes.AuditEventResponse:
      properties:
        action:
          description: |-
            Action is the namespaced action name, e.g. "user.disabled". The set
            grows without a wire change, so a client must render an unknown
            action rather than assume the list is closed.
          type: string
        actor:
          $ref: '#/components/schemas/webtypes.AuditSubjectResponse'
        created_at:
          type: string
        detail:
          additionalProperties: {}
          description: |-
            Detail is the per-action specifics, passed through as decoded JSON so
            a new action's fields reach the UI without a wire type change. Never
            carries a secret.
          type: object
        id:
          type: string
        reason:
          type: string
        system:
          description: |-
            System marks an action taken by the server rather than a person,
            which is how a reader tells "nobody" from "not recorded".
          type: boolean
        target:
          $ref: '#/components/schemas/webtypes.AuditSubjectResponse'
      required:
      - action
      - created_at
      - id
      type: object
    webtypes.AuditEventsResponse:
      properties:
        events:
          items:
            $ref: '#/components/schemas/webtypes.AuditEventResponse'
          type: array
          uniqueItems: false
        next_offset:
          description: NextOffset is the offset for the following page, or 0 on the
            last one.
          type: integer
        total:
          type: integer
      required:
      - events
      type: object
    webtypes.AuditSubjectResponse:
      properties:
        email:
          type: string
        groups:
          items:
            type: string
          type: array
          uniqueItems: false
        subject:
          type: string
        user_id:
          description: |-
            UserID is the grouping key the timelines are built on. It may be
            empty (a system or anonymous actor) and it may no longer match any
            row, which does not make the rest of the snapshot less true.
          type: string
        username:
          type: string
      type: object
    webtypes.BrandingResponse:
      properties:
        login_notice:
          description: |-
            LoginNotice is a plain-text message shown on the login page before authentication.
            Empty disables the notice. Supports newlines for multi-line text.
          type: string
        logo_url:
          description: |-
            LogoURL is generated by the server and points to /api/branding/logo when
            a logo is configured. Always same-origin, never an external URL.
            Omitted entirely when no logo is configured (not set to empty string).
          type: string
        org_name:
          description: |-
            OrgName is the organization name displayed in the web UI (e.g., "Acme Corp").
            Empty disables organization-specific branding.
          type: string
      type: object
    webtypes.CertificateListAdminResponse:
      properties:
        certificates:
          items:
            $ref: '#/components/schemas/webtypes.CertificateResponse'
          type: array
          uniqueItems: false
        page_meta:
          $ref: '#/components/schemas/webtypes.PageMeta'
      required:
      - certificates
      - page_meta
      type: object
    webtypes.CertificateListResponse:
      properties:
        certificates:
          items:
            $ref: '#/components/schemas/webtypes.CertificateResponse'
          type: array
          uniqueItems: false
        next_cursor:
          type: string
      required:
      - certificates
      type: object
    webtypes.CertificateOptionsResponse:
      description: |-
        Options are the certificate options fixed at approval, already
        narrowed by server config: what a redemption actually grants, not
        what the client asked for.
      properties:
        extensions:
          items:
            type: string
          type: array
          uniqueItems: false
        force_command:
          type: string
        no_touch_required:
          type: boolean
        source_addresses:
          items:
            type: string
          type: array
          uniqueItems: false
      required:
      - extensions
      - no_touch_required
      type: object
    webtypes.CertificateResponse:
      properties:
        critical_options:
          additionalProperties:
            type: string
          description: |-
            CriticalOptions are the options fixed into the certificate
            (force-command, source-address). Kept separate from Extensions
            because sshd treats them differently: it rejects a certificate
            carrying a critical option it does not understand, where an unknown
            extension is ignored. Populated by the detail endpoint alone, same as
            Extensions.
          type: object
        decided_accept_language:
          type: string
        decided_at:
          type: string
        decided_by_email:
          type: string
        decided_by_groups:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_other_accounts:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_outcome:
          type: string
        decided_by_service_accounts:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_subject:
          type: string
        decided_by_username:
          type: string
        decided_forwarded_for:
          type: string
        decided_granted_options:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        decided_policy_explanation:
          type: string
        decided_principals:
          description: |-
            The decision's content and reasoning; see RequestDetailResponse.
            Populated on the detail endpoint only.
          items:
            type: string
          type: array
          uniqueItems: false
        decided_source_ip:
          type: string
        decided_user_agent:
          type: string
        enrollment_id:
          description: |-
            EnrollmentID is the service code this certificate was redeemed from,
            so the UI can link to it. Present only on a service certificate.
          type: string
        expires_at:
          type: string
        extensions:
          description: |-
            Extensions are the SSH certificate extensions the certificate was
            signed with (permit-pty, permit-agent-forwarding, ...), decoded from
            the audit row's JSON column rather than passed through as a string:
            the browser should not have to parse JSON out of JSON.

            Populated by the detail endpoint alone. A list row does not display
            them, and carrying them on every row of a hundred-row page would be
            payload nobody reads.
          items:
            type: string
          type: array
          uniqueItems: false
        id:
          type: string
        issued_at:
          type: string
        key_id:
          type: string
        principals:
          type: string
        public_key_fingerprint:
          type: string
        reported_client:
          type: string
        reported_hostname:
          type: string
        reported_machine_id:
          type: string
        reported_pam_service:
          type: string
        reported_process:
          type: string
        reported_remote_host:
          type: string
        reported_requesting_user:
          type: string
        reported_tty:
          type: string
        reported_username:
          description: |-
            What asked for this certificate, as the requester reported it, taken
            from the decision record's snapshot rather than from the request --
            see model.CertificateRequestDecision. Without these the history could
            say who approved a certificate and what it granted but not which
            machine or command it was for, which is the question an incident
            review actually starts from.

            Every one is self-reported by an unauthenticated caller and must be
            rendered as a claim, exactly as the approval page renders it (see
            RequestDetailResponse). The one field here the server established
            itself is DecidedSourceIP above, and that is the approver's, not the
            requester's.

            ReportedUsername and ReportedHostname are the "user@host" that asked:
            the PAM account and machine for a pam or console certificate, the
            local client's for a user one. The rest are PAM and console only, and
            empty on a user certificate, which has no service or terminal to
            report. All are populated by the detail endpoint alone.
          type: string
        retrieved_at:
          description: |-
            RetrievedAt is when that redemption happened. Distinct from IssuedAt
            only by the width of the signing round trip, and carried because the
            retrieval log is timestamped by it — matching a certificate to a line
            in that log means comparing like with like.
          type: string
        retrieved_source_ip:
          description: |-
            RetrievedSourceIP is the address the `service retrieve` call came
            from — the machine the unattended job actually ran on. Present only
            on a service certificate.

            It answers a different question from DecidedSourceIP below, which for
            a service certificate is the approver's browser at enrollment time
            and is therefore identical across every certificate the code mints.
            This one is this certificate's own origin.
          type: string
        serial_number:
          description: |-
            SerialNumber is a decimal string, not a JSON number. Serials are 63
            bits of randomness (internal/serial), so all but a vanishing fraction
            exceed JavaScript's Number.MAX_SAFE_INTEGER (2^53-1) and a browser
            parsing one as a number silently rounds it -- 3260700569889958163
            reads back as 3260700569889958400. On an audit record that is a wrong
            answer, and an unsearchable one: the serial an operator reads off
            `ssh-keygen -L` never matches what is on screen.

            Both tags are needed. `json:",string"` decides what Go writes;
            `tstype` decides what tygo tells the browser to expect, which it
            otherwise infers from the Go type and gets wrong.
          example: "0"
          type: string
        type:
          $ref: '#/components/schemas/model.CertificateType'
      required:
      - expires_at
      - id
      - issued_at
      - key_id
      - principals
      - public_key_fingerprint
      - serial_number
      - type
      type: object
    webtypes.ConfigSection:
      properties:
        name:
          description: |-
            Name is the top-level key the section covers ("http", "cert_options"),
            or "server" for the switches that sit at the root of the file.
          type: string
        settings:
          description: Settings are the section's leaves, in the order they are declared.
          items:
            $ref: '#/components/schemas/webtypes.ConfigSetting'
          type: array
          uniqueItems: false
      required:
      - name
      - settings
      type: object
    webtypes.ConfigSetting:
      properties:
        key:
          description: |-
            Key is the dotted configuration key, e.g. "http.tls.min_version" —
            the path an operator would write in their own config file, so a
            value on this screen can be traced back to the line that set it.
          type: string
        secret:
          description: |-
            Secret marks a key whose value is never sent. Value is the redaction
            placeholder when a secret is configured and empty when none is, so
            an operator can still tell whether one is set.
          type: boolean
        value:
          description: |-
            Value is what is in effect, rendered as text. Empty means unset: an
            empty string, a nil pointer, or an empty list or map.
          type: string
      required:
      - key
      type: object
    webtypes.CurrentUserResponse:
      properties:
        email:
          type: string
        extra:
          additionalProperties: {}
          description: |-
            Extra holds operator-configured extra fields captured at login from
            OIDC claims (see config.OAuthFields.Extra). Each value is either a
            string or an array of strings, reflecting the claim shape at login.
            Missing or null values in this field are rendered as "MISSING" by key
            ID templates, so this presence is important for debugging when
            debugging a key ID template that expected a claim that did not arrive.
            The frontend should display missing values visibly rather than hiding them.
          type: object
        groups:
          items:
            type: string
          type: array
          uniqueItems: false
        is_admin:
          description: |-
            IsAdmin, IsSOC and IsAuditor report the access levels this session
            holds, so the UI can show the affordances each one unlocks. The roles
            nest — an admin holds all three, a SOC member holds SOC and auditor —
            so more than one is true at a time and the account page names every
            one rather than only the narrowest. Display only: the server re-checks
            the matching Grants* rule on every scoped request, so hiding or
            showing an affordance changes nothing about what this session can do.
          type: boolean
        is_auditor:
          type: boolean
        is_soc:
          type: boolean
        name:
          description: |-
            Name is the caller's human-readable name, for greeting them as a
            person rather than as an account. Display only, and empty when
            neither the identity provider nor the directory supplied one.
          type: string
        other_accounts:
          description: |-
            OtherAccounts are alternate account identifiers this identity is
            known by on target systems (see config.OAuthFields.OtherAccounts),
            shown so a user can see every account name tied to their identity.
            These are also the candidates, alongside Username, that the approval
            page offers as principals for a user certificate (see
            ApproveRequestBody.Principals).
          items:
            type: string
          type: array
          uniqueItems: false
        service_accounts:
          description: |-
            ServiceAccounts are the service accounts this identity may approve
            service certificates for (see config.OAuthFields.ServiceAccounts) —
            the approval page's picker is populated from them.
          items:
            type: string
          type: array
          uniqueItems: false
        subject:
          type: string
        username:
          type: string
      required:
      - email
      - extra
      - groups
      - is_admin
      - is_auditor
      - is_soc
      - name
      - other_accounts
      - service_accounts
      - subject
      - username
      type: object
    webtypes.DiagnosticCheckResult:
      properties:
        findings:
          description: |-
            Findings are the individual observations, already phrased for an
            operator. Empty when the check found nothing to report.
          items:
            type: string
          type: array
          uniqueItems: false
        id:
          description: ID is a stable machine key for the check, e.g. "reachability".
          type: string
        remediation:
          description: Remediation is the concrete fix. Empty when the status is "ok".
          type: string
        status:
          description: |-
            Status is the worst finding's severity: "ok", "warn", "critical", or
            "skipped".
          type: string
        summary:
          description: Summary is the one-line headline for the check.
          type: string
        title:
          description: Title is the human name of the check.
          type: string
      required:
      - id
      - status
      - summary
      - title
      type: object
    webtypes.DiagnosticsResponse:
      properties:
        checks:
          description: Checks are the individual results, in display order.
          items:
            $ref: '#/components/schemas/webtypes.DiagnosticCheckResult'
          type: array
          uniqueItems: false
        public_origin:
          description: |-
            PublicOrigin is the URL the edge checks probed, echoed so the operator
            can confirm the run tested what they expected. Empty when
            http.public_url is not set.
          type: string
      type: object
    webtypes.DisableUserConsequences:
      properties:
        service_enrollment_count:
          description: |-
            ServiceEnrollmentCount is how many live enrollments this user
            approved. They are unaffected — reported so the dialog can say so
            with a number, which is the reassurance an admin disabling a
            colleague actually needs.
          type: integer
      required:
      - service_enrollment_count
      type: object
    webtypes.DisableUserRequestBody:
      properties:
        reason:
          description: |-
            Reason explains why the user is being disabled. Required and
            server-validated (non-empty, length-capped): the next admin opening
            this account needs to learn why it was disabled, and an optional
            field does not get filled.
          type: string
      required:
      - reason
      type: object
    webtypes.EffectiveConfigResponse:
      properties:
        sections:
          items:
            $ref: '#/components/schemas/webtypes.ConfigSection'
          type: array
          uniqueItems: false
      required:
      - sections
      type: object
    webtypes.EnrollmentRetrievalResponse:
      properties:
        certificate_serial:
          description: |-
            A decimal string on the wire, same as CertificateResponse.SerialNumber
            and for the same reason.
          example: "0"
          type: string
        retrieved_at:
          type: string
        source_ip:
          type: string
        succeeded:
          description: |-
            Succeeded is false for a redemption that passed code validation but
            failed at signing — still worth surfacing: someone held the code.
          type: boolean
      required:
      - certificate_serial
      - retrieved_at
      - source_ip
      - succeeded
      type: object
    webtypes.EnrollmentRetrievalsResponse:
      properties:
        retrievals:
          items:
            $ref: '#/components/schemas/webtypes.EnrollmentRetrievalResponse'
          type: array
          uniqueItems: false
        total:
          description: |-
            Total counts every logged redemption, not just the ones returned, so
            the UI can say what it is showing a slice of rather than implying the
            page is the whole history.
          type: integer
      required:
      - retrievals
      - total
      type: object
    webtypes.ExpireEnrollmentRequestBody:
      properties:
        reason:
          description: |-
            Reason explains why the enrollment is being expired. Required and
            server-validated, like the user containment reasons.
          type: string
      required:
      - reason
      type: object
    webtypes.IdentityEchoStartResponse:
      properties:
        authorization_url:
          description: |-
            AuthorizationURL carries prompt=login, so the provider issues a fresh
            token rather than replaying its own session.
          type: string
      required:
      - authorization_url
      type: object
    webtypes.LDAPProbeAttribute:
      properties:
        configured:
          description: |-
            Configured reports that a configured field reads this attribute,
            which is what the console highlights.
          type: boolean
        name:
          type: string
        truncated_values:
          description: TruncatedValues is how many values the probe's own cap dropped.
          type: integer
        values:
          items:
            type: string
          type: array
          uniqueItems: false
      required:
      - name
      - values
      type: object
    webtypes.LDAPProbeBindings:
      description: Bindings are the typed values used when BindingSource is "custom".
      properties:
        email:
          type: string
        extra:
          additionalProperties:
            type: string
          type: object
        subject:
          type: string
        username:
          type: string
      type: object
    webtypes.LDAPProbeEntry:
      properties:
        attributes:
          items:
            $ref: '#/components/schemas/webtypes.LDAPProbeAttribute'
          type: array
          uniqueItems: false
        dn:
          type: string
      required:
      - attributes
      - dn
      type: object
    webtypes.LDAPProbeField:
      properties:
        attribute:
          description: |-
            Attribute is the entry attribute the field reads, absent for a
            search-only field.
          type: string
        attribute_present:
          description: |-
            AttributePresent distinguishes an attribute that is empty from one
            that is not on the entry at all — which is usually a typo.
          type: boolean
        attribute_values:
          items:
            type: string
          type: array
          uniqueItems: false
        error:
          type: string
        name:
          type: string
        searches:
          items:
            $ref: '#/components/schemas/webtypes.LDAPProbeSearch'
          type: array
          uniqueItems: false
        values:
          description: |-
            Values is what the field resolved to, as the login path would
            compute it.
          items:
            type: string
          type: array
          uniqueItems: false
      required:
      - name
      - values
      type: object
    webtypes.LDAPProbeMerge:
      properties:
        action:
          description: Action is "override", "persist-groups" or "extra".
          type: string
        dropped:
          items:
            type: string
          type: array
          uniqueItems: false
        kept:
          description: |-
            Kept and Dropped split the values by the group allowlist. Dropped is
            only ever non-empty for the group field.
          items:
            type: string
          type: array
          uniqueItems: false
        name:
          type: string
        note:
          type: string
      required:
      - action
      - name
      type: object
    webtypes.LDAPProbeRequestBody:
      properties:
        attributes:
          description: |-
            Attributes are the attribute names to request. Empty requests every
            user attribute, which is the point of the console.
          items:
            type: string
          type: array
          uniqueItems: false
        binding_source:
          description: |-
            BindingSource selects where the template bindings come from: "self"
            (the calling admin's own identity, the default), "user" (an existing
            user named by UserID), or "custom" (the typed Bindings below).
          type: string
        bindings:
          $ref: '#/components/schemas/webtypes.LDAPProbeBindings'
        filter:
          description: |-
            Filter is the filter to run. Empty in template mode means the
            configured ldap.user_filter.
          type: string
        mode:
          description: |-
            Mode is "template" (render Filter against the bindings, with RFC 4515
            escaping applied) or "literal" (send Filter exactly as typed).
            Defaults to template.
          type: string
        user_id:
          description: UserID names the user to bind against when BindingSource is
            "user".
          type: string
      type: object
    webtypes.LDAPProbeResponse:
      properties:
        attributes:
          items:
            type: string
          type: array
          uniqueItems: false
        base_dn:
          description: |-
            BaseDN, FilterSent, Mode and Attributes are the request as it went
            out, so an operator sees the rendered filter rather than the
            template.
          type: string
        directory_id:
          type: string
        elapsed_ms:
          type: integer
        entry:
          $ref: '#/components/schemas/webtypes.LDAPProbeEntry'
        fields:
          items:
            $ref: '#/components/schemas/webtypes.LDAPProbeField'
          type: array
          uniqueItems: false
        filter_sent:
          type: string
        id_attribute:
          description: |-
            IDAttribute echoes ldap.id_attribute and DirectoryID is what it
            resolved to on the matched entry — the value that would be stored as
            the re-anchoring identifier. Both empty when it is unconfigured, in
            which case Suggestions names a candidate the entry actually carries.
          type: string
        matched:
          description: |-
            Matched is how many entries the filter found, capped by the probe.
            The login path refuses anything but exactly one.
          type: integer
        merge:
          items:
            $ref: '#/components/schemas/webtypes.LDAPProbeMerge'
          type: array
          uniqueItems: false
        mode:
          type: string
        suggestions:
          items:
            $ref: '#/components/schemas/webtypes.LDAPProbeSuggestion'
          type: array
          uniqueItems: false
        timeout_ms:
          type: integer
        tls_insecure_skip_verify:
          description: |-
            TLSInsecureSkipVerify reports that the connection did not verify the
            directory certificate.
          type: boolean
        wrote:
          description: |-
            Wrote is always false and is serialized anyway: the guarantee is part
            of the response, not only of the documentation.
          type: boolean
      required:
      - attributes
      - base_dn
      - filter_sent
      - mode
      type: object
    webtypes.LDAPProbeSearch:
      properties:
        base_dn:
          description: BaseDN and FilterSent are what actually went to the directory.
          type: string
        entries:
          type: integer
        error:
          type: string
        filter_sent:
          type: string
        name:
          type: string
        value:
          description: Value is the attribute read off each matched entry.
          type: string
        values:
          items:
            type: string
          type: array
          uniqueItems: false
      required:
      - name
      type: object
    webtypes.LDAPProbeSuggestion:
      properties:
        reason:
          type: string
        yaml:
          type: string
      required:
      - reason
      - yaml
      type: object
    webtypes.LDAPStatusResponse:
      properties:
        base_dn:
          type: string
        configured_attributes:
          description: |-
            ConfiguredAttributes are the attribute names the configured fields
            read, which is what the console highlights in a returned entry.
          items:
            type: string
          type: array
          uniqueItems: false
        disable_after_seconds:
          description: |-
            DisableAfterSeconds is how long an entry may stay missing before the
            user is auto-disabled. Zero means never.
          type: integer
        enabled:
          description: |-
            Enabled is false when ldap.enabled is off, in which case everything
            below is unset and there is nothing to probe.
          type: boolean
        last_run:
          $ref: '#/components/schemas/webtypes.LDAPSyncRunResponse'
        reenable:
          description: |-
            Reenable reports whether the sync clears its own disables when an
            entry reappears.
          type: boolean
        running:
          description: |-
            Running reports a pass in progress on the instance that answered
            this request. Passes are not leader-elected, so another instance may
            be running one too.
          type: boolean
        sync_interval_seconds:
          description: |-
            SyncIntervalSeconds is zero when the scheduled sync is off, which is
            itself the answer to "why has nothing synced".
          type: integer
        tls_insecure_skip_verify:
          description: |-
            TLSInsecureSkipVerify reports that directory connections do not
            verify the server certificate. Reported rather than silently
            honoured: a probe that succeeds only because verification is off has
            to say so.
          type: boolean
        url:
          description: |-
            URL, BaseDN and UserFilter are the connection and query the probe is
            pinned to. The probe cannot be re-pointed, so these are the whole
            target.
          type: string
        user_filter:
          type: string
      type: object
    webtypes.LDAPSyncRequestBody:
      properties:
        dry_run:
          description: |-
            DryRun reads the directory and reports what the pass would do,
            changing nothing. It is what makes the button safe to press during an
            incident.
          type: boolean
      type: object
    webtypes.LDAPSyncRunResponse:
      description: |-
        LastRun is the most recent pass on any instance, absent when none has
        ever run.
      properties:
        actor_username:
          description: |-
            ActorUsername is the admin who triggered it, absent for a scheduled
            pass. The username rather than the id, since it is displayed.
          type: string
        disabled:
          type: integer
        dry_run:
          type: boolean
        error:
          description: |-
            Error is why the pass could not run — an unreachable directory, a
            failed bind. Empty for a pass that completed, whatever it concluded
            about individual users.
          type: string
        failed:
          type: integer
        finished_at:
          description: |-
            FinishedAt is absent while the pass runs, which is also how a pass
            that died with its process reads afterwards.
          type: string
        found:
          type: integer
        id:
          type: string
        instance:
          description: |-
            Instance is the host that ran it. Jobs are not leader-elected, so
            every instance runs its own pass and this says whose log to read.
          type: string
        missing:
          type: integer
        reenabled:
          type: integer
        started_at:
          type: string
        trigger:
          description: Trigger is "schedule" or "manual".
          type: string
        users_seen:
          type: integer
      required:
      - id
      - started_at
      - trigger
      type: object
    webtypes.NotificationKindResponse:
      properties:
        description:
          description: Description is the sentence explaining when this one fires.
          type: string
        enabled:
          description: |-
            Enabled is this user's answer, or the kind's default when they have
            never given one.
          type: boolean
        kind:
          description: Kind is the stable identifier, the key the update body uses.
          type: string
        title:
          description: Title is the short label shown beside the toggle.
          type: string
      required:
      - description
      - kind
      - title
      type: object
    webtypes.NotificationPreferencesResponse:
      properties:
        address:
          description: |-
            Address is where notifications would be sent, from the users table.
            Empty when the identity provider releases no email claim — the other
            reason nothing arrives, and equally worth showing.
          type: string
        kinds:
          description: |-
            Kinds is every notification the server knows how to send, in a
            stable order.
          items:
            $ref: '#/components/schemas/webtypes.NotificationKindResponse'
          type: array
          uniqueItems: false
        mail_enabled:
          description: |-
            MailEnabled reports whether the server can send mail at all. False
            means the toggles are still recorded but nothing is delivered, which
            the page says out loud rather than leaving the user to infer.
          type: boolean
      required:
      - kinds
      type: object
    webtypes.PageMeta:
      properties:
        limit:
          description: |-
            Limit is the page size actually served, which may be smaller than the
            one asked for (see paging.MaxLimit).
          type: integer
        offset:
          description: Offset is how many rows were skipped to reach this page.
          type: integer
        page:
          description: |-
            Page is the 1-based number of this page, and PageCount how many pages
            Total fills at this size. Both are derivable from the three fields
            above, and are sent anyway so every list UI computes them the same way
            — including the "an empty list is page 1 of 1" boundary.
          type: integer
        page_count:
          type: integer
        total:
          description: Total is how many rows match the request's filter, across all
            pages.
          type: integer
      required:
      - limit
      - offset
      - page
      - page_count
      - total
      type: object
    webtypes.ReEnableUserRequestBody:
      properties:
        reason:
          description: |-
            Reason explains why the user is being re-enabled, e.g. "cleared with
            security, SEC-1234". Required on the same terms as the disable
            reason, and as valuable to the person after this one.
          type: string
      required:
      - reason
      type: object
    webtypes.RequestDetailResponse:
      properties:
        already_closed:
          type: boolean
        approval_url:
          type: string
        caller_gid:
          type: integer
        caller_pid:
          type: integer
        caller_ppid:
          type: integer
        caller_uid:
          type: integer
        client:
          type: string
        client_mode:
          type: string
        client_time:
          type: string
        created_at:
          type: string
        decided_accept_language:
          type: string
        decided_at:
          type: string
        decided_by_email:
          type: string
        decided_by_groups:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_other_accounts:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_outcome:
          type: string
        decided_by_service_accounts:
          items:
            type: string
          type: array
          uniqueItems: false
        decided_by_subject:
          type: string
        decided_by_username:
          type: string
        decided_forwarded_for:
          type: string
        decided_granted_options:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        decided_policy_explanation:
          type: string
        decided_principals:
          description: |-
            What the approval granted and why: the principals selected, the
            options after every narrowing, and the lifetime policy's own
            explanation as a JSON document (see service.PolicyExplanation).
            Approvals only; absent for denials and for decisions predating the
            columns.
          items:
            type: string
          type: array
          uniqueItems: false
        decided_source_ip:
          type: string
        decided_user_agent:
          type: string
        expires_at:
          description: |-
            ExpiresAt is when the request stops being approvable, from its own
            type's budget. The page counts down to it, which matters most for
            console requests: their budget is deliberately the shortest, and an
            approver who cannot see the clock cannot tell a slow OIDC login from
            a request that has already died.
          type: string
        granted:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        hostname:
          description: |-
            The console context the request carried: which machine it claims to
            be, through which PAM service, at which terminal, and what it
            reported as the remote host.

            Every one of them is self-reported by an unauthenticated caller and
            the UI must render them as claims rather than as facts — they are
            what lets a human notice "I am at my desk, why is there a console
            login on rack07", not what authorizes anything. A non-empty
            RemoteHost on a console request is worth flagging outright: a real
            console has no remote host.

            Set for PAM and console requests; empty for the others.
          type: string
        id:
          type: string
        is_owned_by_you:
          type: boolean
        local_hostname:
          type: string
        local_username:
          type: string
        machine_id:
          type: string
        os:
          type: string
        pam_service:
          type: string
        principals:
          items:
            type: string
          type: array
          uniqueItems: false
        process:
          type: string
        public_key:
          type: string
        remote_host:
          type: string
        requested:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        requesting_user:
          description: |-
            The rest of the host context, same trust: who invoked the service
            (PAM_RUSER), the command line asking, the process identifiers, a
            stable machine id, the platform, the module and its configured
            mode, the host's clock, and the CA fingerprints the host trusts.
            See apitypes.PAMRequestBody for each. Set for PAM and console
            requests; the numeric ones are absent rather than zero when the
            module did not report them.
          type: string
        source_ip:
          type: string
        status:
          $ref: '#/components/schemas/model.CertificateRequestStatus'
        target_account:
          description: |-
            TargetAccount is the local account a PAM or console request is
            authenticating: who `sudo` is being run as, or the account typed at
            the `login:` prompt. Empty for every other type. It is
            reported by an unauthenticated client and never becomes a principal
            (see model.CertificateRequest.Username), so the UI must present it as
            what is being attempted rather than as what is being granted. Without
            it the approver cannot see which account the sudo is for, since the
            principals now describe the approver instead.
          type: string
        trusted_ca_fingerprints:
          items:
            type: string
          type: array
          uniqueItems: false
        tty:
          type: string
        type:
          $ref: '#/components/schemas/model.CertificateType'
        valid_seconds:
          type: integer
      required:
      - already_closed
      - approval_url
      - created_at
      - expires_at
      - granted
      - id
      - is_owned_by_you
      - principals
      - public_key
      - requested
      - source_ip
      - status
      - type
      - valid_seconds
      type: object
    webtypes.ResolveCodeRequestBody:
      properties:
        code:
          type: string
      required:
      - code
      type: object
    webtypes.ResolveCodeResponse:
      properties:
        approval_url:
          description: |-
            ApprovalURL is the page to send the browser to. Returned rather than
            assembled client-side so the one definition of the path stays on the
            server, alongside the create response's.
          type: string
        request_id:
          type: string
      required:
      - approval_url
      - request_id
      type: object
    webtypes.ServiceEnrollmentResponse:
      properties:
        approved_by_username:
          description: |-
            ApprovedByUsername is who approved this code. Provenance, not
            ownership — the reader may well not be them. Empty if that user's
            record has since gone.
          type: string
        certificate_request_id:
          description: |-
            CertificateRequestID is the request this enrollment was approved
            from, and the id the retrieval log at
            /api/certs/requests/{id}/retrievals is keyed on. Omitted for an
            enrollment with no request linked to it.
          type: string
        certificate_valid_seconds:
          description: |-
            CertificateValidSeconds is how long each redeemed certificate is
            valid for, measured from its own redemption rather than from
            approval. Omitted for an enrollment created before the code and
            certificate lifetimes were split, where ExpiresAt bounded both.
          type: integer
        created_at:
          description: CreatedAt is when the enrollment was approved.
          type: string
        expires_at:
          description: |-
            ExpiresAt bounds the code, not the certificates it produces: past it
            `service retrieve` stops redeeming, and the unattended job behind it
            needs a fresh enrollment.
          type: string
        first_redeemed_at:
          description: |-
            FirstRedeemedAt is the first successful redemption, absent for a code
            that has never produced a certificate.
          type: string
        id:
          type: string
        key_id:
          description: |-
            KeyID is likewise fixed at approval and lands verbatim in every
            certificate, which is what a target host's logs record.
          type: string
        last_retrieved_at:
          description: |-
            LastRetrievedAt is the most recent redemption attempt, successful or
            not. Together with RetrievalCount it is how the approver tells a code
            still driving a cron job from one nothing has used in months.
          type: string
        notification_email:
          description: |-
            NotificationEmail is where notifications about this enrollment go
            instead of to every holder of the service account. Empty means they
            fan out, which is the default. Any holder may change it.
          type: string
        options:
          $ref: '#/components/schemas/webtypes.CertificateOptionsResponse'
        principals:
          description: |-
            Principals is what every certificate this code produces carries. For
            a service enrollment that is the single service account the approver
            picked, fixed at approval time and never re-derived at redemption.
          items:
            type: string
          type: array
          uniqueItems: false
        public_key_fingerprint:
          description: |-
            PublicKeyFingerprint identifies the keypair the code is bound to.
            `service retrieve` never sends a public key, so a code lifted without
            this keypair produces nothing usable.

            Empty if the stored key could not be parsed — a display gap, not a
            reason to withhold the rest of the row.
          type: string
        retrieval_count:
          description: |-
            RetrievalCount counts every logged redemption attempt, including
            those that failed at signing — someone held the code either way.
          type: integer
        service_account:
          description: |-
            ServiceAccount is the account this code was approved for, and who
            owns it: everyone holding the account (see
            docs/proposals/enrollment-group-ownership.md). It is what the service
            codes page groups by, which is why it is its own field rather than
            left to be read out of Principals.
          type: string
      required:
      - created_at
      - expires_at
      - id
      - key_id
      - options
      - principals
      - retrieval_count
      - service_account
      type: object
    webtypes.ServiceEnrollmentsResponse:
      properties:
        enrollments:
          items:
            $ref: '#/components/schemas/webtypes.ServiceEnrollmentResponse'
          type: array
          uniqueItems: false
      required:
      - enrollments
      type: object
    webtypes.SetNotificationEmailRequestBody:
      properties:
        notification_email:
          description: |-
            NotificationEmail is the address every notification about this
            enrollment goes to. Empty clears it, restoring fan-out to every
            holder of the service account.

            No omitempty: an absent field and an empty one must mean the same
            thing here, because clearing the address is the whole reason to send
            an empty one.
          type: string
      type: object
    webtypes.UpdateNotificationPreferencesBody:
      properties:
        kinds:
          additionalProperties:
            type: boolean
          description: |-
            Kinds maps a notification kind to whether it should be sent. An
            unknown key is rejected rather than ignored: silently dropping it
            would report success for a preference that was never stored.
          type: object
      required:
      - kinds
      type: object
    webtypes.VersionResponse:
      properties:
        commit:
          description: |-
            Commit is the git revision the build came from. It is what identifies
            a "development" build, which has no release of its own to point at.
          type: string
        github_url:
          description: |-
            GithubURL is the project's source repository. Served rather than
            hardcoded in the frontend so that a fork only has to change
            internal/version.
          type: string
        release_url:
          description: |-
            ReleaseURL points at the GitHub release matching Version. Omitted for
            an untagged build, where there is no release page to link to.
          type: string
        version:
          description: |-
            Version is the release this binary was built from, without the tag's
            leading "v" (goreleaser and the Makefile both strip it). Untagged
            builds report the "development" default from internal/version.
          type: string
      required:
      - commit
      - github_url
      - version
      type: object
  securitySchemes:
    sessionCookie:
      description: The session established by /auth/callback. Sent by the browser
        automatically; the ssoossh client never has one, and the endpoints it calls
        do not require one.
      in: cookie
      name: ssoossh_session
      type: apiKey
externalDocs:
  description: ""
  url: ""
info:
  contact:
    name: ssoossh
    url: https://github.com/mnestor/ssoossh
  description: |-
    SSO for SSH. Two audiences share this surface:

    - **The client** (`ssoossh`, and `pam_ssoossh`) creates certificate requests and waits on the result over SSE. These endpoints are unauthenticated: a request's ID is an unguessable UUID and is itself the capability, and authorization happens at the approval step.
    - **The web UI** reads and approves. Everything under a session cookie.

    **Every** JSON body this API emits is the `{data, error}` envelope from `.claude/rules/server-api.md` — success and error, client-facing and web-UI-facing, including the data of each server-sent event. One decode path, no exceptions to remember.

    This file is generated from Go annotations by `make openapi`. Edit the handlers, not the YAML.
  license:
    name: MIT
  title: ssoosshd API
  version: "0.1"
openapi: 3.1.0
paths:
  /api/admin/audit:
    get:
      description: |-
        Returns the administrative audit stream newest first. The
        database copy is a bounded cache of recent events; the
        shipped audit log is the archive, and searching happens
        there rather than here.
      parameters:
      - description: Page size (default 25, max 100)
        example: 25
        in: query
        name: limit
        schema:
          type: integer
      - description: Results to skip (default 0)
        example: 0
        in: query
        name: offset
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.AuditEventsResponse'
          description: Audit events
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid paging parameters
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: View the recent audit event feed (auditor-only)
      tags:
      - admin
  /api/admin/certificates/history:
    get:
      description: |-
        Returns issued certificates across all users, useful for audits,
        incident review, and tracking "who issued this?". Supports
        searching over key ID, principals, serial number, fingerprint, and owner
        username/email, filtering by type and expiration status, and offset pagination.
      parameters:
      - description: Number of results (default 25, max 100)
        example: 25
        in: query
        name: limit
        schema:
          type: integer
      - description: Number of results to skip (default 0)
        example: 0
        in: query
        name: offset
        schema:
          type: integer
      - description: Search term (max 200 chars)
        example: alice
        in: query
        name: q
        schema:
          type: string
      - description: Filter by certificate type (user/service/pam/console)
        example: user
        in: query
        name: type
        schema:
          type: string
      - description: Filter by expiration (live/expired)
        example: live
        in: query
        name: status
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CertificateListAdminEnvelope'
          description: Certificates and page metadata
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid parameters
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: View certificate history across all users (auditor-only)
      tags:
      - admin
  /api/admin/config:
    get:
      description: |-
        Returns every key of the server's effective configuration,
        grouped by top-level section, with secrets redacted (CA key,
        HSM PIN, client secret, cookie signing key, database
        connection string, LDAP bind password, SMTP password). A
        redacted key still reports whether a value is set. Useful for
        debugging and audit trails.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.EffectiveConfigResponse'
          description: Current effective configuration
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: View effective server configuration (auditor-only)
      tags:
      - admin
  /api/admin/diagnostics/run:
    post:
      description: |-
        Runs the deployment self-checks and returns what they found:
        whether the server can reach its own public URL, how the
        client IP is resolved through http.trusted_proxies, whether
        the edge weakens the security headers the app sets, and
        whether the edge adds dangerous CORS headers. The edge checks
        call the server's own public_url back through whatever sits in
        front of it; the target is always public_url and cannot be
        supplied by the caller. Nothing is written.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.DiagnosticsResponse'
          description: What the checks found
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Too many runs
      security:
      - sessionCookie: []
      summary: Run deployment self-checks (admin-only)
      tags:
      - admin
  /api/admin/enrollments:
    get:
      description: |-
        Returns a paged list of all service enrollments across users, searchable
        by approver name, principals, key ID, or request ID. Visible to auditors.
      parameters:
      - description: Page size (default 25, max 100)
        example: 25
        in: query
        name: limit
        schema:
          type: integer
      - description: Rows to skip (default 0)
        example: 0
        in: query
        name: offset
        schema:
          type: integer
      - description: Search query (max 200 chars)
        example: '"alice"'
        in: query
        name: q
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.AdminEnrollmentsResponse'
          description: Paged enrollment list
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: List all service enrollments (auditor-only)
      tags:
      - admin
  /api/admin/enrollments/{id}:
    get:
      description: |-
        Returns full details of one enrollment including its retrieval log
        and any historical reassignments. Visible to auditors and to
        holders of the enrollment's service account.
      parameters:
      - description: Enrollment ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/gin.H'
          description: Full enrollment details
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Enrollment not found
      security:
      - sessionCookie: []
      summary: Get enrollment details (auditor-only)
      tags:
      - admin
  /api/admin/enrollments/{id}/expire:
    patch:
      description: |-
        Immediately marks an enrollment as expired, preventing future
        service certificate retrievals. The operation is idempotent.
      parameters:
      - description: Enrollment ID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webtypes.ExpireEnrollmentRequestBody'
              description: Expiry reason (required)
              summary: request
        description: Expiry reason (required)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/gin.H'
          description: Enrollment expired
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin or SOC
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Enrollment not found
      security:
      - sessionCookie: []
      summary: Expire an enrollment (admin or SOC)
      tags:
      - admin
  /api/admin/identity/echo/start:
    post:
      description: |-
        Returns the authorization URL for a fresh OIDC round trip
        carrying prompt=login. The callback renders the decoded ID
        token instead of establishing a session, and the caller's
        existing session is left exactly as it was.

        The echo re-authenticates rather than remembering: the server
        keeps only the claims the configuration maps, so there is no
        stored copy of the rest to show. Nothing about the result is
        written down — it reaches the page in the redirect fragment,
        which never reaches the server at all.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.IdentityEchoStartResponse'
          description: Where to send the browser
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin
      security:
      - sessionCookie: []
      summary: Start a claims echo (admin-only)
      tags:
      - admin
  /api/admin/ldap/probe:
    post:
      description: |-
        Runs one directory lookup through the login path's three
        stages — lookup, field resolution, then the merge and
        allowlist — and stops before the write. Reports the filter it
        actually sent, every attribute the directory returned rather
        than the ones the configuration names, what each field
        resolved to, which group values the allowlist would drop, and
        the config lines that would keep what is currently ignored.

        The connection is not part of the request: the probe always
        uses the running ldap.url, bind credentials and base_dn, so
        it cannot be pointed at another host. It writes nothing.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.LDAPProbeRequestBody'
                description: What to ask the directory
                summary: request
        description: What to ask the directory
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.LDAPProbeResponse'
          description: What the probe found
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid request, filter or bindings
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The user to bind against does not exist
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Too many probes
      security:
      - sessionCookie: []
      summary: Probe the directory (admin-only, read-only)
      tags:
      - admin
  /api/admin/ldap/status:
    get:
      description: |-
        Reports whether directory enrichment is enabled, the
        connection and query the probe is pinned to, the sync
        settings, and the most recent sync pass on any instance.
        Names no credential: the bind password is never part of this
        response. Answers "is the sync even running", which before
        the run record could only be answered by reading a log on
        whichever instance happened to run it.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.LDAPStatusResponse'
          description: Directory status
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: Directory sync status and probe target (auditor-only)
      tags:
      - admin
  /api/admin/ldap/sync:
    post:
      description: |-
        Runs the same pass the scheduler runs, on this instance, and
        returns what it concluded. One at a time: a pass already in
        progress makes this a 409 rather than stacking a second one
        over the same users. With dry_run the pass reads the
        directory and changes nothing, reporting the disables and
        re-enables it would have performed — which is what makes it
        safe to press during an incident.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.LDAPSyncRequestBody'
                description: Sync options
                summary: request
        description: Sync options
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.LDAPSyncRunResponse'
          description: What the pass concluded
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid request body
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: A sync is already running
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The directory could not be reached
      security:
      - sessionCookie: []
      summary: Run the directory sync now (admin-only)
      tags:
      - admin
  /api/admin/users:
    get:
      description: |-
        Returns a paginated list of users, searchable by username,
        email, or subject. Useful for user directory and audit.
      parameters:
      - description: Page size (default 25, max 100)
        example: 25
        in: query
        name: limit
        schema:
          type: integer
      - description: Results to skip (default 0)
        example: 0
        in: query
        name: offset
        schema:
          type: integer
      - description: Search term
        example: alice
        in: query
        name: q
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.AdminUsersListResponse'
          description: User list
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid paging/search parameters
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: List all users (auditor-only)
      tags:
      - admin
  /api/admin/users/{id}:
    get:
      description: |-
        Returns full identity details, disable state, and counts of
        certificates and service enrollments for one user.
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.AdminUserDetail'
          description: User details
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: User not found
      security:
      - sessionCookie: []
      summary: Get user details (auditor-only)
      tags:
      - admin
  /api/admin/users/{id}/audit:
    get:
      description: |-
        Returns the audit events where this user is the actor or the
        target, newest first. One row serves both sides: a disable
        shows on the analyst's history and on the disabled account's
        page.
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Page size (default 25, max 100)
        example: 25
        in: query
        name: limit
        schema:
          type: integer
      - description: Results to skip (default 0)
        example: 0
        in: query
        name: offset
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.AuditEventsResponse'
          description: Audit events
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid paging parameters
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as auditor
      security:
      - sessionCookie: []
      summary: View a user's audit timeline (auditor-only)
      tags:
      - admin
  /api/admin/users/{id}/disable:
    patch:
      description: |-
        Marks a user as disabled, preventing authentication. Service
        enrollments they approved are unaffected. The operation is
        idempotent.
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webtypes.DisableUserRequestBody'
              description: Disable reason (required)
              summary: request
        description: Disable reason (required)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webtypes.DisableUserConsequences'
          description: Consequences of disabling
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin or SOC
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: User not found
      security:
      - sessionCookie: []
      summary: Disable a user (admin or SOC)
      tags:
      - admin
  /api/admin/users/{id}/enable:
    patch:
      description: |-
        Re-enables a previously disabled user. The operation is idempotent.
        Note: service enrollments that have already expired are not
        un-expired; the user must request new enrollments.
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webtypes.ReEnableUserRequestBody'
              description: Re-enable reason (required)
              summary: request
        description: Re-enable reason (required)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/gin.H'
          description: User re-enabled
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authenticated
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not authorized as admin
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: User not found
      security:
      - sessionCookie: []
      summary: Re-enable a user (admin-only)
      tags:
      - admin
  /api/branding:
    get:
      description: |-
        Unauthenticated endpoint that returns optional branding for the login page
        and web UI. All fields are optional; empty values mean no branding is
        configured. Returns an empty object if no branding is configured.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.BrandingEnvelope'
          description: Branding configuration (may be empty)
      summary: Get branding configuration
      tags:
      - public
  /api/branding/logo:
    get:
      description: |-
        Serves the logo image configured by the operator. The image is read
        and validated at startup, so this handler simply streams the bytes with
        appropriate cache headers.
      responses:
        "200":
          content:
            image/gif:
              schema:
                type: file
            image/jpeg:
              schema:
                type: file
            image/png:
              schema:
                type: file
            image/svg+xml:
              schema:
                type: file
            image/webp:
              schema:
                type: file
          description: Logo image
      summary: Get organization logo image
      tags:
      - public
  /api/ca:
    get:
      description: |-
        For `TrustedUserCAKeys` and `@cert-authority` lines. Public by design —
        it is a public key.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CAEnvelope'
          description: The CA public key in authorized_keys form
      summary: The CA public key
      tags:
      - client
  /api/certs:
    get:
      description: |-
        Metadata only. Certificates themselves are ephemeral and never persisted,
        so this is an audit trail rather than somewhere to re-download one.

        Scoped by the caller's users row, with no parameter to widen it.
        Uses cursor-based pagination (not offset).
      parameters:
      - description: Certificate ID to start after (cursor)
        in: query
        name: after
        schema:
          type: string
      - description: Maximum certificates to return (default 25, max 100)
        in: query
        name: limit
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CertificateListEnvelope'
          description: Issued certificates, newest first, with cursor for next page
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid limit or cursor
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
      security:
      - sessionCookie: []
      summary: The caller's issued-certificate history
      tags:
      - web
  /api/certs/{id}:
    get:
      description: |-
        Returns a single certificate by ID if the caller is authorized:
        the user who approved the underlying request, or someone with auditor-level access.
        Returns 404 uniformly for "not found" and "not authorized" to not leak existence.
      parameters:
      - description: Certificate ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CertificateDetailEnvelope'
          description: Certificate details
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Invalid certificate ID
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Certificate not found or not accessible
      security:
      - sessionCookie: []
      summary: View a certificate's full details
      tags:
      - web
  /api/certs/console:
    post:
      description: |-
        Unauthenticated. Returns a short `user_code` for a human to type into the
        web UI, the page that accepts it, and `expires_at` — the deadline the
        client should bound its own wait by.

        Username is the local account being logged into, reported by the caller
        and used for display and audit only. The certificate's principals are
        accounts the approver holds and selects at approval; the module on the host
        matches them against that local account, directly or through its
        principals-map. The remaining context fields are equally self-reported and
        are shown to the approver as claims.

        Set `cert_options.console.require` to restrict who may approve one, and
        `cert_options.console.allowed_networks` to refuse creation from outside
        named networks — that refusal happens here, before any human is asked.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/apitypes.ConsoleRequestBody'
                description: The public key to sign, the local account being logged
                  into, and the console context
                summary: request
        description: The public key to sign, the local account being logged into,
          and the console context
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CreateRequestEnvelope'
          description: Request created
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Malformed body, or a public key that will not parse
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The source address is outside cert_options.console.allowed_networks
      summary: Create a console login certificate request
      tags:
      - client
  /api/certs/pam:
    post:
      description: |-
        Unauthenticated. Username is the local account pam_ssoossh is authenticating,
        reported by the caller and used for display and audit only. The certificate's
        principals are accounts the approver holds and selects at approval; the module
        on the host matches them against that local account, directly or through its
        principals-map. Set `cert_options.pam.require` to restrict who may approve one.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/apitypes.PAMRequestBody'
                description: The public key to sign, the local username being authenticated,
                  and the options being asked for
                summary: request
        description: The public key to sign, the local username being authenticated,
          and the options being asked for
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CreateRequestEnvelope'
          description: Request created
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Malformed body, or a public key that will not parse
      summary: Create a PAM certificate request
      tags:
      - client
  /api/certs/requests/{id}:
    get:
      description: |-
        The approval page's data source.

        Returns `requested` and `granted` separately on purpose: server config is
        the outer bound on every option and trims rather than rejects, so a human
        has to be able to see what is being dropped before they approve.

        **This binds the request to the caller.** It is the first authenticated
        touch a request gets, so opening the page claims it; a different user gets
        403 here rather than after clicking approve.
      parameters:
      - description: The certificate request's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.RequestDetailEnvelope'
          description: Request detail
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Already bound to another user
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No such request
      security:
      - sessionCookie: []
      summary: What the caller would be approving
      tags:
      - web
  /api/certs/requests/{id}/approve:
    post:
      description: |-
        Session-authed and CSRF-guarded. Publishes a signing job; the certificate
        reaches the client over its own SSE stream, not in this response.

        Approving a service-type request requires a body naming which of the
        approver's own service accounts the certificate is for; that account
        becomes the certificate principal. Other types take no body.
      parameters:
      - description: The certificate request's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.ApproveRequestBody'
                description: Service-account selection (service-type requests only)
                summary: request
        description: Service-account selection (service-type requests only)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ApproveEnvelope'
          description: Queued for signing
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Bound to another user, or a cross-origin call
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No such request
      security:
      - sessionCookie: []
      summary: Approve a request
      tags:
      - web
  /api/certs/requests/{id}/deny:
    post:
      description: |-
        Session-authed and CSRF-guarded. The waiting client is told over its own
        SSE stream.
      parameters:
      - description: The certificate request's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.DenyEnvelope'
          description: Denied
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Bound to another user, or a cross-origin call
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No such request
      security:
      - sessionCookie: []
      summary: Deny a request
      tags:
      - web
  /api/certs/requests/{id}/events:
    get:
      description: |-
        A real `text/event-stream`, separate from the creating POST because an
        EventSource-style client is GET-only. Safe to reconnect any number of
        times.

        The SSE event *name* carries the terminal status (`approved`, `denied`,
        `expired`, `enrolled`, `failed`); each event's data is an envelope whose
        `data` half carries the certificate, or the enrollment code together with
        the service account it was approved for and when the code expires. That
        shape is not in this document: the response body is a stream rather than
        JSON, so there is nowhere to declare a schema for what individual events
        carry.

        Unauthenticated: the request ID is the credential.
      parameters:
      - description: The certificate request's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                type: string
          description: The stream, closed after one terminal event
        "404":
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No such request
        "410":
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The certificate was issued but is no longer available. They
            are never persisted, so a client that missed delivery must re-request.
      summary: Wait for a request's outcome (SSE)
      tags:
      - client
  /api/certs/requests/{id}/retrievals:
    get:
      description: |-
        Redemptions of the enrollment created from this service request —
        when, from where, and whether a certificate was issued. Codes are
        reusable, so the log accumulates one row per redemption; the newest 100
        are returned and "total" reports how many exist. Visible to the
        enrollment's approving user and to auditors.
      parameters:
      - description: The certificate request's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.EnrollmentRetrievalsEnvelope'
          description: The retrieval log
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Neither the approving user nor an auditor
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No enrollment for this request
      security:
      - sessionCookie: []
      summary: A service enrollment's retrieval log
      tags:
      - web
  /api/certs/requests/resolve-code:
    post:
      description: |-
        Session-authed and CSRF-guarded. Turns the code a human read off a
        console screen into the request it names, and claims that request for
        the submitting session.

        **Authentication is not optional here.** An unauthenticated caller must
        never learn whether a code is live and must never receive a request ID:
        the ID is the credential the certificate is delivered against.

        The three failure modes are distinct on purpose, because they send the
        user to three different next actions: 404 no such code (retype it), 410
        expired (start the login again at the machine), 403 already claimed by
        another session.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.ResolveCodeRequestBody'
                description: The code as typed
                summary: request
        description: The code as typed
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ResolveCodeEnvelope'
          description: Resolved and claimed
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not a well-formed code
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Claimed by another user, or a cross-origin call
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No live request carries that code
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The request expired before the code was submitted
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Too many code submissions
      security:
      - sessionCookie: []
      summary: Resolve a console login code
      tags:
      - web
  /api/certs/service/enroll:
    post:
      description: |-
        Approving one yields an enrollment code rather than a certificate; the
        certificate comes later from `/api/certs/service/retrieve`. This is the
        one path where an approval does not queue a signing job.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/apitypes.ServiceEnrollRequestBody'
                description: The service public key and the options being asked for
                summary: request
        description: The service public key and the options being asked for
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CreateRequestEnvelope'
          description: Request created
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Malformed body, or a public key that will not parse
      summary: Create a service enrollment request
      tags:
      - client
  /api/certs/service/enrollments:
    get:
      description: |-
        What each enrollment code will hand out and how long it stays
        redeemable: the principals and options fixed at approval, the keypair it
        is bound to, when it was approved, when it stops working, and how often
        it has been redeemed.

        The code itself is never returned. `service enroll` prints it once, and
        a browser session is not a way to mint service certificates.

        Scoped by the caller's users row, with no parameter to widen it.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ServiceEnrollmentsEnvelope'
          description: Approved enrollments, newest first
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
      security:
      - sessionCookie: []
      summary: The caller's approved service enrollments
      tags:
      - web
  /api/certs/service/enrollments/{id}/notification-email:
    patch:
      description: |-
        Points every notification about this enrollment at one address instead
        of fanning out to every holder of its service account. An empty value
        clears it and restores fan-out.

        This exists for the cases fan-out cannot serve: a service account whose
        holders have never logged in reaches nobody, an identity provider that
        releases no email claim silences every holder, and a large holder set
        turns every redemption into a mailshot where a team alias would do.

        Allowed to any holder of the enrollment's service account, and to SOC
        operators. Auditor is not enough: it is a read role, and this write
        redirects every future message about a credential.
      parameters:
      - description: The enrollment's UUID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.SetNotificationEmailRequestBody'
                description: The address, or empty to clear it
                summary: request
        description: The address, or empty to clear it
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.SetNotificationEmailEnvelope'
          description: The stored address
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not a valid email address
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Not a holder of the service account
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No such enrollment
      security:
      - sessionCookie: []
      summary: Set an enrollment's notification address
      tags:
      - web
  /api/certs/service/retrieve:
    post:
      description: |-
        Signs and returns a service certificate for the enrollment the code
        identifies, using the public key and options fixed at approval time.
        Codes are reusable until the enrollment expires; every redemption is
        logged for the approving user and auditors. The certificate is valid
        from now until the enrollment's expiry.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/apitypes.RetrieveRequestBody'
                description: The enrollment code to redeem
                summary: request
        description: The enrollment code to redeem
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.RetrieveEnvelope'
          description: The signed service certificate
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Unknown or expired enrollment code
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Signing failed or timed out
      summary: Redeem an enrollment code for a certificate
      tags:
      - client
  /api/certs/user:
    post:
      description: |-
        Unauthenticated. Returns two relative URLs: `events_url` to wait on, and
        `approval_url` for a human to open. The request ID is the capability —
        it is an unguessable UUID, and holding it is what authorizes waiting on
        the outcome.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/apitypes.UserRequestBody'
                description: The public key to sign, and the options being asked for
                summary: request
        description: The public key to sign, and the options being asked for
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CreateRequestEnvelope'
          description: Request created
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Malformed body, or a public key that will not parse
      summary: Create a user certificate request
      tags:
      - client
  /api/users/me:
    get:
      description: Sourced primarily from the session, with extra fields hydrated
        from the users table.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.CurrentUserEnvelope'
          description: The session identity
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
      security:
      - sessionCookie: []
      summary: The caller's own identity
      tags:
      - web
  /api/users/me/notifications:
    get:
      description: |-
        Every notification kind this server knows how to send, with the
        caller's answer for each. Kinds the caller has never answered report
        the kind's own default. Also reports whether the server is configured
        to send mail at all, and the address it would send to.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.NotificationPreferencesEnvelope'
          description: The caller's notification preferences
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The session has no user record
      security:
      - sessionCookie: []
      summary: The caller's own notification preferences
      tags:
      - web
    put:
      description: |-
        Sets the caller's answer for each notification kind named in the body,
        leaving every other kind unchanged. An unrecognized kind is rejected
        rather than ignored, and nothing is stored when one is present.
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/webtypes.UpdateNotificationPreferencesBody'
                description: The kinds to change
                summary: request
        description: The kinds to change
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.NotificationPreferencesEnvelope'
          description: The preferences after saving
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Malformed body or an unrecognized notification kind
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: No valid session
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: The session has no user record
      security:
      - sessionCookie: []
      summary: Update the caller's own notification preferences
      tags:
      - web
  /api/version:
    get:
      description: |-
        Unauthenticated endpoint that returns the running server's build
        identity for display in the web UI footer. What it reveals is
        governed by version.mode: "show" (the real build), "hide"
        (nothing), or "fake" (a fixed string). Values come from the
        build stamp; an untagged build reports "development" and omits the
        release URL.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.VersionEnvelope'
          description: Build identity of the running server
      summary: Get server version
      tags:
      - public
  /auth/callback:
    get:
      description: |-
        Checks `state` against the value stored at login, exchanges the code,
        establishes the session, and redirects to the captured `return_to` (or
        `/`).
      parameters:
      - description: Authorization code from the identity provider
        in: query
        name: code
        required: true
        schema:
          type: string
      - description: CSRF state value stored at login
        in: query
        name: state
        required: true
        schema:
          type: string
      responses:
        "302":
          content:
            application/json:
              schema:
                type: string
          description: Redirect back into the app
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: State mismatch, or the provider rejected the exchange
      summary: OIDC callback
      tags:
      - auth
  /auth/login:
    get:
      description: |-
        Redirects the browser to the identity provider. `return_to` is validated
        as a same-site, path-only relative URL; anything else is ignored rather
        than followed.
      parameters:
      - description: Where to land after login
        example: /approve/9f1c0b2a-...
        in: query
        name: return_to
        schema:
          type: string
      responses:
        "302":
          content:
            application/json:
              schema:
                type: string
          description: Redirect to the identity provider
      summary: Start the OIDC login flow
      tags:
      - auth
  /auth/logout:
    post:
      description: |-
        Clears the server-side session, which is what actually revokes the cookie
        — `SessionAuthMiddleware` fails closed without an identity, so a stale
        cookie left in the browser grants nothing.

        CSRF-guarded. No session required: logging out an already-logged-out
        caller is a no-op.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.LogoutEnvelope'
          description: Logged out
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.ErrorEnvelope'
          description: Cross-origin call
      summary: End the session
      tags:
      - auth
  /healthz:
    get:
      description: |-
        Registered ahead of the server-name check, so a probe addressing the
        server by IP rather than by its configured name still reaches it.

        Note this is one of the two endpoints that does not use the {data, error}
        envelope: it predates the convention and is consumed by orchestrators
        rather than by this project's own clients.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/openapidoc.HealthPayload'
          description: The server is up
      summary: Liveness probe
  /ping:
    get:
      description: For probes that want a body they can string-match without parsing
        JSON.
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: pong
      summary: Liveness probe, plain text
tags:
- description: Called by the ssoossh client, unauthenticated
  name: client
- description: Called by the web UI, session-authenticated
  name: web
- description: Browser-facing OIDC redirects
  name: auth
