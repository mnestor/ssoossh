{
  "name": "${localWorkspaceFolderBasename}-client",
  "service": "client", // container name from docker-compose
  "dockerComposeFile": [
    "../../docker-compose.yml",
    "../docker-compose.yml",
    "../docker-compose.local.yml"
  ],
  "workspaceFolder": "/workspace/ssoossh",
  "features": {
    "ghcr.io/devcontainers/features/node:2.1.0": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "initializeCommand": "scripts/devcontainer-init.sh",
  "shutdownAction": "none",
  "postCreateCommand": {
    // "install-node": "bash -c '. \"$NVM_DIR/nvm.sh\" && nvm install node'",
    "allow-git": "git config --global --add safe.directory /workspace/ssoossh"
  },
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "settings": {
        "todo-tree.ripgrep.ripgrep": "/usr/bin/rg",
        "dev.containers.copyGitConfig": true
      },
      "extensions": [
        "davidanson.vscode-markdownlint",
        "github.copilot-chat",
        "golang.go",
        "ms-azuretools.vscode-docker", // Docker integration and linting
        "shardulm94.trailing-spaces", // Show trailing spaces
        "Gruntfuggly.todo-tree", // Highlights TODO comments
        "dnut.rewrap-revived", // rewrap comments after n characters on one line
        "vscode-icons-team.vscode-icons", // Better file extension icons
        "redhat.vscode-yaml", // Kubernetes, Drone syntax highlighting
        "bajdzis.vscode-database", // Supports connections to mysql or postgres, over SSL, socked
        "IBM.output-colorizer", // Colorize your output/test logs
        "EditorConfig.EditorConfig",
        "anthropic.claude-code"
      ]
    }
  }
}