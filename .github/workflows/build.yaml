name: build

# The release pipeline, plus the cheap check that stands in for it on PRs.
#
# On a v* tag push this publishes the real release (goreleaser
# archives/packages, the ssoosshd Docker image). PRs no longer run that
# pipeline: they run `build-check`, which does the generated-artifact
# staleness checks and a single-target snapshot build, with no release
# secrets on the runner. The weekly run is what still exercises the full
# multi-platform signed pipeline against upstream drift (new goreleaser,
# new base images), which is the job the PR dry-run was really doing and
# was paying for on every push to do it.
on:
  workflow_dispatch:
  push:
    tags:
      - v*
  pull_request:
  schedule:
    # The only run that exercises the full signed multi-platform pipeline
    # outside an actual release. Kept while the other weekly duplicates
    # were dropped, because this one is not a duplicate of anything a PR
    # does.
    - cron: "0 4 * * 1" # 04:00 UTC Monday

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Path gate: on PRs, skip the expensive build jobs when nothing that
  # feeds the release changed. Implemented as a filter job rather than
  # `on.pull_request.paths` so a required check still reports (as skipped,
  # which satisfies branch protection) instead of hanging in "expected".
  # On non-PR events the filter step is skipped and the fallback in the
  # output expression makes everything run.
  changes:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code || 'true' }}
    steps:
      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        if: github.event_name == 'pull_request'
        id: filter
        with:
          filters: |
            code:
              - '**/*.go'
              - 'go.mod'
              - 'go.sum'
              - 'frontend/**'
              - 'Makefile'
              - 'Dockerfile'
              - 'packaging/**'
              - '.goreleaser*.yml'
              - 'docs/openapi.yaml'
              - 'tygo.yaml'
              - '.redoclyrc.yaml'
              - '.github/workflows/build.yaml'

  # What a PR gets instead of the release pipeline: the checks that catch a
  # committed artifact going stale, plus proof that the tree still builds
  # through goreleaser. One target (linux/amd64) rather than four platforms,
  # and no 1Password/GPG/notarization secrets -- a PR branch does not need
  # the signing key to prove the config parses.
  build-check:
    name: build check
    needs: changes
    if: github.event_name == 'pull_request' && needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: ghcr.io/mnestor/ssoossh-build:latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      # Catches a Go wire type changing without the frontend's generated
      # TypeScript being regenerated.
      - name: Check generated frontend types are current
        run: make types-check

      # docs/openapi.yaml is generated from the swag annotations on the
      # handlers. Catches a route or response shape changing without the
      # spec following.
      - name: Check generated OpenAPI spec is current
        run: make openapi-check

      - name: Lint OpenAPI spec
        run: make openapi-lint

      # Man pages are generated from the cobra commands; same staleness
      # trap as the two above.
      - name: Check generated man pages are current
        run: make man-check

      # The docs site's configuration reference comes from the config
      # structs and its CLI reference from the cobra commands. Catches a
      # key or a flag changing without the site following.
      - name: Check generated docs site pages are current
        run: make confdocs-check clidocs-check

      # The server binary embeds server/frontend/dist.
      - name: Build web UI
        run: make frontend

      - name: Snapshot build (linux/amd64)
        run: |
          git config --global --add safe.directory "$(pwd)"
          goreleaser build --single-target --snapshot --clean -f .goreleaser.yml
        env:
          GOOS: linux
          GOARCH: amd64

  build-most:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: changes
    if: github.event_name != 'pull_request' && needs.changes.outputs.code == 'true'
    permissions:
      # goreleaser creates the release and uploads assets on tag pushes.
      contents: write
      # goreleaser's dockers_v2 pipe pushes ghcr.io/mnestor/ssoosshd on tag
      # pushes (see .goreleaser.yml).
      packages: write
    container:
      image: ghcr.io/mnestor/ssoossh-build:latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      # Catches a Go wire type changing without the frontend's generated
      # TypeScript being regenerated. The golden test in server/webtypes
      # catches the same drift against docs/openapi.yaml and the goldens
      # themselves; this half is what keeps the browser's types honest.
      - name: Check generated frontend types are current
        run: make types-check

      # docs/openapi.yaml is generated from the swag annotations on the
      # handlers. Catches a route or response shape changing without the
      # spec following.
      - name: Check generated OpenAPI spec is current
        run: make openapi-check

      # Validate the OpenAPI spec against the OpenAPI 3.1 specification.
      - name: Lint OpenAPI spec
        run: make openapi-lint

      # Man pages are generated from the cobra commands. A stale page must
      # not ship in a release archive.
      - name: Check generated man pages are current
        run: make man-check

      # The docs site's generated reference pages, for the same reason.
      - name: Check generated docs site pages are current
        run: make confdocs-check clidocs-check

      # Must run before goreleaser: the server binary embeds
      # server/frontend/dist, and only a placeholder index.html is tracked.
      # Skipping this would ship a release with no web UI.
      - name: Build web UI
        run: make frontend

      # Also before goreleaser, which attaches the result as a release asset
      # (.goreleaser.yml's release.extra_files). An explicit step rather than
      # a goreleaser before-hook for the same reason the web UI is one: a
      # hook would make every local snapshot build produce it too.
      #
      # The script refuses to build a bundle whose manifest is stale, so a
      # release cannot ship fixtures that disagree with the version
      # github.com/mnestor/ssoossh-pam pins against them.
      - name: Build the wire-contract bundle
        run: make wire-contract-bundle

      # 0.16 is a floor, not a preference: earlier zig cc accepted -dynamic
      # on a musl target and linked libc statically anyway, and a static musl
      # binary cannot dlopen a PKCS#11 module (see .goreleaser.yml). Keep in
      # sync with ZIG_VERSION in .github/docker/Dockerfile.runner. The
      # tarball name changed shape in 0.15 (zig-x86_64-linux-, not
      # zig-linux-x86_64-).
      - name: Set up Zig
        env:
          ZIG_VERSION: "0.16.0"
        run: |
          mkdir -p /tmp/zig
          cd /tmp/zig
          wget -q "https://ziglang.org/download/${ZIG_VERSION}/zig-x86_64-linux-${ZIG_VERSION}.tar.xz"
          tar -xf "zig-x86_64-linux-${ZIG_VERSION}.tar.xz"
          echo "/tmp/zig/zig-x86_64-linux-${ZIG_VERSION}" >> "$GITHUB_PATH"

      # goreleaser's dockers_v2 pipe shells out to `docker buildx build
      # --platform linux/amd64,linux/arm64 ... --push` as one call per
      # image (see .goreleaser.yml) -- that needs a docker-container
      # buildx builder already active, which is what this action's default
      # driver sets up; the plain docker driver cannot push a multi-arch
      # manifest at all. docker itself is on PATH via
      # .github/docker/Dockerfile.runner; the daemon is the host's, reached
      # over the /var/run/docker.sock GitHub-hosted runners mount into a
      # container: job automatically.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      # Tag pushes only, matching every other publish step below -- a
      # snapshot build never reaches goreleaser's publish phase (it skips
      # publishing automatically in --snapshot mode), so logging in here
      # would be dead weight on every other push.
      - name: Log in to GHCR
        if: startsWith(github.ref, 'refs/tags/')
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: 1password/load-secrets-action@70062d7a876d3eb6334754fa26efd2fbd90c32f2 # v5.0.1
        id: secrets
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          GPG_PASSPHRASE: "op://Github Actions/ifx7xxoazokhouv2z2cstg7f7a/password"
          QUILL_NOTARY_ISSUER: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_ISSUER
          QUILL_NOTARY_KEY: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_KEY
          QUILL_NOTARY_KEY_ID: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_KEY_ID
          QUILL_SIGN_P12: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_SIGN_P12
          QUILL_SIGN_PASSWORD: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_SIGN_PASSWORD

      - name: Save gpg key
        run: |
          op read "op://Github Actions/ifx7xxoazokhouv2z2cstg7f7a/private.pgp" > "${GITHUB_WORKSPACE}/private.pgp"
          git config --global --add safe.directory "$(pwd)"
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          distribution: goreleaser
          # Latest stable v2 release, not nightly: releases should not
          # depend on an unreleased goreleaser build.
          version: "~> v2"
          args: release --clean -f .goreleaser.yml --snapshot=${{ !startsWith(github.ref, 'refs/tags/') }} ${{ vars.DEBUG }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NFPM_PASSPHRASE: ${{ steps.secrets.outputs.GPG_PASSPHRASE }}
          QUILL_NOTARY_ISSUER: ${{ steps.secrets.outputs.QUILL_NOTARY_ISSUER }}
          QUILL_NOTARY_KEY: ${{ steps.secrets.outputs.QUILL_NOTARY_KEY }}
          QUILL_NOTARY_KEY_ID: ${{ steps.secrets.outputs.QUILL_NOTARY_KEY_ID }}
          QUILL_SIGN_P12: ${{ steps.secrets.outputs.QUILL_SIGN_P12 }}
          QUILL_SIGN_PASSWORD: ${{ steps.secrets.outputs.QUILL_SIGN_PASSWORD }}

      - name: Verify glibc floor (GNU build)
        run: |
          set -euo pipefail
          GLIBC_MAX=$(objdump -T dist/server-linux-build_linux_amd64_v1/ssoosshd 2>/dev/null | grep -o 'GLIBC_[0-9.]*' | sort -V | tail -1)
          echo "Maximum GLIBC version in ssoosshd: $GLIBC_MAX"
          if [ "$GLIBC_MAX" = "GLIBC_2.28" ]; then
            echo "✓ glibc floor is 2.28 (expected)"
          elif [ -z "$GLIBC_MAX" ]; then
            echo "✗ Could not determine GLIBC version (static binary?)"
            exit 1
          else
            echo "✗ GLIBC version $GLIBC_MAX is newer than 2.28 floor"
            exit 1
          fi

      # Both arches, not just amd64: a toolchain that quietly drops -dynamic
      # (see .goreleaser.yml) drops it for arm64 too, and an Alpine package
      # whose PKCS#11 support is dead on arrival looks identical from here.
      - name: Verify musl builds are dynamically linked
        run: |
          set -euo pipefail
          found=0
          for MUSL_BINARY in dist/server-linux-musl-build_linux_*/ssoosshd; do
            [ -f "$MUSL_BINARY" ] || continue
            found=$((found + 1))
            FILE_OUTPUT=$(file "$MUSL_BINARY")
            echo "File output: $FILE_OUTPUT"
            if echo "$FILE_OUTPUT" | grep -q "dynamically linked"; then
              echo "✓ $MUSL_BINARY is dynamically linked (supports dlopen)"
            else
              echo "✗ $MUSL_BINARY is statically linked (cannot dlopen PKCS#11 modules)"
              exit 1
            fi
          done
          if [ "$found" -eq 0 ]; then
            echo "✗ no musl binaries under dist/server-linux-musl-build_linux_*/"
            exit 1
          fi

      # Run the binary rather than docker run alpine: even though this job
      # now has docker (for goreleaser's dockers_v2 step above), installing
      # Ubuntu's musl package and running the binary directly is simpler
      # than a throwaway alpine container just for this check, and it
      # verifies the same thing -- the package installs
      # /lib/ld-musl-x86_64.so.1, the exact interpreter path zig wrote into
      # the ELF header, so the loader here is the one Alpine would use.
      # Linking is checked above; this catches a binary that links but
      # cannot start. Installed here rather than baked into the runner
      # image on purpose: a release must not depend on that image having
      # been republished first.
      - name: Smoke test musl binary
        run: |
          set -euo pipefail
          apt-get update -qq
          apt-get install -y -qq --no-install-recommends musl
          MUSL_BINARY="dist/server-linux-musl-build_linux_amd64_v1/ssoosshd"
          "$MUSL_BINARY" version || { echo "✗ musl binary failed to run under the musl loader"; exit 1; }
          echo "✓ musl binary runs under the musl loader"

      # What the macos-client-pkg job below wraps as installer packages.
      # Handing it the archives rather than the release assets is what lets
      # that job run on a snapshot too: nothing is published in --snapshot
      # mode, so a job that downloaded from the release would only ever run
      # during an actual release -- and the weekly run exists precisely so
      # the signed pipeline is exercised before then.
      - name: Upload the darwin client archives
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: darwin-client-archives
          path: dist/ssoossh-client_*_darwin_*.zip
          if-no-files-found: error
          retention-days: 7

      - name: cleanup
        if: always()
        run: |
          rm -f private.pgp || true

      - name: remove the release, we failed
        if: startsWith(github.ref, 'refs/tags/') && failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release delete -y "$TAG"

  # The macOS client installer, one package per architecture.
  #
  # A second job on a Mac rather than a step in build-most, because
  # pkgbuild, productbuild, productsign and notarytool exist only on macOS
  # and that job runs in a Linux container. Nothing is compiled here: it
  # takes the darwin archives build-most uploaded, whose binaries goreleaser
  # already had quill sign and notarize, and wraps each one untouched.
  #
  # It runs on every event build-most does, not only on tags. On a snapshot
  # the packages are still signed and notarized -- quill's post-hook runs in
  # the build phase, which --snapshot does not skip -- and simply never
  # reach a release. That is what keeps the weekly run an honest rehearsal
  # of this half of the pipeline instead of it first executing during a real
  # release.
  macos-client-pkg:
    name: macos client pkg
    needs: [changes, build-most]
    if: github.event_name != 'pull_request' && needs.changes.outputs.code == 'true'
    # Pinned to a version rather than macos-latest, like every other version
    # in this pipeline. Nothing is compiled here, so this is not an SDK
    # choice: the newest image is simply the one whose notarytool and
    # stapler are the ones Apple is currently changing, and the weekly run
    # exists to meet those changes before a release does.
    #
    # It does not set the package's floor. allowed-os-versions comes from
    # the client binary's LC_BUILD_VERSION minos, which packaging/macos/pkg.sh
    # reads out of the binary itself, so the floor follows the Go toolchain
    # and not this label.
    runs-on: macos-26
    timeout-minutes: 60
    permissions:
      # gh release upload, on tag pushes only.
      contents: write
    steps:
      # packaging/macos/ holds the script, Distribution.xml and the
      # installer's HTML panes; the payload comes from the artifact below.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: darwin-client-archives
          path: dist

      # The same 1Password item build-most reads for quill. QUILL_INSTALLER_P12
      # is the field quill itself does not use: a Developer ID *Application*
      # certificate signs a Mach-O and cannot sign an installer package, and
      # Gatekeeper refuses a downloaded package that is not Installer-signed.
      - uses: 1password/load-secrets-action@70062d7a876d3eb6334754fa26efd2fbd90c32f2 # v5.0.1
        id: secrets
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          QUILL_INSTALLER_P12: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_INSTALLER_P12
          QUILL_SIGN_PASSWORD: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_SIGN_PASSWORD
          QUILL_NOTARY_ISSUER: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_ISSUER
          QUILL_NOTARY_KEY: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_KEY
          QUILL_NOTARY_KEY_ID: op://Github Actions/7cyb7oo2m4hp3cmqnxeexghy2a/QUILL_NOTARY_KEY_ID

      - name: Installer packages, signed and notarized
        env:
          QUILL_INSTALLER_P12: ${{ steps.secrets.outputs.QUILL_INSTALLER_P12 }}
          QUILL_SIGN_PASSWORD: ${{ steps.secrets.outputs.QUILL_SIGN_PASSWORD }}
          QUILL_NOTARY_ISSUER: ${{ steps.secrets.outputs.QUILL_NOTARY_ISSUER }}
          QUILL_NOTARY_KEY: ${{ steps.secrets.outputs.QUILL_NOTARY_KEY }}
          QUILL_NOTARY_KEY_ID: ${{ steps.secrets.outputs.QUILL_NOTARY_KEY_ID }}
        run: |
          set -euo pipefail
          for archive in dist/ssoossh-client_*_darwin_*.zip; do
            packaging/macos/pkg.sh "$archive" dist
          done
          ls -l dist/*.pkg

      # Both architectures must be here. A glob that matched one archive
      # because the other never got built would otherwise ship a release
      # with half its installers and no failure anywhere.
      - name: Both architectures were packaged
        run: |
          set -euo pipefail
          for arch in amd64 arm64; do
            ls dist/ssoossh-client_*_darwin_"$arch".pkg >/dev/null
          done

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-client-packages
          path: dist/*.pkg
          if-no-files-found: error
          retention-days: 7

      # goreleaser created the release in build-most and has finished with
      # it, so these go up afterwards with gh. The checksums file it wrote
      # is rewritten to cover them too -- a release whose checksums list
      # describes only some of its assets is worse than one with none.
      # Existing lines for these names are dropped first, so a re-run of
      # this job replaces them rather than appending a second set.
      - name: Attach the packages to the release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          gh release upload "$TAG" dist/*.pkg --clobber

          sums=$(gh release view "$TAG" --json assets \
            --jq '.assets[].name | select(endswith("_checksums.txt"))')
          if [ -z "$sums" ]; then
            echo "no checksums asset on $TAG; nothing to amend"
            exit 0
          fi
          gh release download "$TAG" --pattern "$sums" --dir dist --clobber
          (
            cd dist
            shasum -a 256 -- *.pkg > pkg-sums.txt
            # Every line of the release's file whose second field is not one
            # of the names just hashed, then those names appended once. The
            # sort is only so the result is stable between runs.
            awk 'NR == FNR { seen[$2] = 1; next } !($2 in seen)' \
              pkg-sums.txt "$sums" > kept.txt
            cat kept.txt pkg-sums.txt | sort -k2 > "$sums"
            rm -f kept.txt pkg-sums.txt
          )
          gh release upload "$TAG" "dist/$sums" --clobber
          cat "dist/$sums"

      # Same invariant build-most keeps: a published release is complete or
      # it is not published. A failure here leaves the tag, not a release
      # missing its macOS installers.
      - name: remove the release, we failed
        if: startsWith(github.ref, 'refs/tags/') && failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release delete -y "$TAG"
