name: codecover

# Unit test suite plus Codecov upload. Runs on hosted runners; the
# e2e/pam/resilience suites are tagged out of `./...` so this stays the
# fast whole-repo unit gate.
#
# push-to-main exists for the Codecov upload: PR runs never load secrets
# (see the upload steps below), so mainline pushes are the only event that
# feeds the dashboard.
on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # No `changes` path gate: the gate job costs about as much as a fraction
  # of this job, and coverage on every PR is the point of the workflow.
  coverage:
    name: codecheck
    # Job-level grants for octocov, which runs on the default GITHUB_TOKEN:
    # pull-requests to comment the report, actions to store and replace the
    # baseline report in the Actions artifact datastore (upstream's own
    # example uses write; read is not enough to delete the previous
    # artifact). This widens what a same-repo PR's token can do -- comment
    # and manage workflow runs -- but exposes no secrets; fork PRs still
    # get a read-only token from GitHub regardless of this block. Job-level
    # permissions replace the workflow-level block, so contents: read is
    # restated here.
    permissions:
      contents: read
      pull-requests: write
      actions: write
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: ghcr.io/mnestor/ssoossh-build:latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Replaces the module/build caching setup-go provided before the Go
      # toolchain moved into the runner image.
      - name: Cache Go modules
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-${{ runner.os }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            go-${{ runner.os }}-

      - name: Install dependencies
        run: go mod download

      # server/frontend embeds server/frontend/dist and nothing there is
      # tracked, so the Go build cannot compile until the UI exists.
      - name: Build web UI
        run: make frontend

      - name: Run tests
        run: CGO_ENABLED=1 go test -buildvcs=false -v -covermode=atomic -coverprofile=coverage.txt ./...

      # Separate run, and it is not optional. Every file in pam_ssoossh/
      # carries //go:build pam -- sources and tests alike -- so the run above
      # compiles the package to nothing. It contributed neither numerator nor
      # denominator to coverage.txt, which meant PAM's real coverage reached
      # no dashboard and a regression in it moved no number. `make test-pam`
      # ran the tests but passed no -coverprofile, so nothing measured them.
      #
      # PAM is the authorization decision for sudo. It is the last part of
      # this repo that should be invisible to coverage.
      #
      # `!cancelled()` for the same reason the two steps below carry it: a
      # failure in the run above used to skip this one, which left the floors
      # step with no PAM profile to read and killed it on a missing file
      # instead of reporting the coverage it did have.
      - name: Run PAM tests
        if: ${{ !cancelled() }}
        run: CGO_ENABLED=1 go test -buildvcs=false -v -tags=pam -covermode=atomic -coverprofile=coverage-pam.txt ./pam_ssoossh/...

      # The ratchet. Coverage regressions are about direction, not position:
      # client/cmd fell 88.7% -> 76.6% over a fortnight with nothing deleted,
      # just new code landing under-tested, and no gate noticed. Reuses the
      # profiles the two steps above already produced rather than running the
      # suite a third time.
      #
      # `!cancelled()` so a coverage drop is still reported when some other
      # step failed -- the two are usually the same change.
      - name: Check coverage floors
        if: ${{ !cancelled() }}
        run: |
          # Into a copy, never coverage.txt itself: the Codecov upload below
          # takes both files, so appending the PAM profile to the main one
          # would count those packages twice.
          cp coverage.txt coverage-combined.txt
          # The PAM profile is absent only when that step failed to build or
          # produce one. Say so, and still check every other floor rather than
          # dying here on `tail`; pam_ssoossh then reports as MISSING.
          if [ -f coverage-pam.txt ]; then
            tail -n +2 coverage-pam.txt >> coverage-combined.txt
          else
            echo "coverage-pam.txt is missing; the PAM step produced no profile"
          fi
          ./scripts/check-coverage-floors.sh coverage-combined.txt

      # octocov reads coverage-combined.txt (see .octocov.yml): on PRs it
      # comments the report with a diff against the baseline stored from
      # main, on main pushes it stores that baseline. Default GITHUB_TOKEN
      # only, so it runs where the Codecov upload below cannot.
      #
      # The condition reads as "not a PR, or a PR from this repo": it runs
      # on same-repo PRs (head.repo matches) and on push/dispatch (the
      # event_name clause). The only thing it excludes is a PR from a
      # fork, where the read-only token cannot comment and octocov would
      # fail the job over it; the floors gate above still runs there.
      #
      # `!cancelled()` so a floors failure still gets its coverage comment
      # -- that is the run where the numbers matter most.
      - name: Report coverage (octocov)
        if: ${{ !cancelled() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
        uses: k1LoW/octocov-action@a167dc0dee441b7ffc45e1b862ab55ec0d87f278 # v1.5.2

      # `!cancelled()` on this step too, not just the upload: a failing
      # test run used to stop the job before the token was ever loaded,
      # which silently skipped the upload the condition below was meant to
      # keep alive.
      #
      # No secrets on pull_request runs at all -- not just forks. PR code is
      # unreviewed by definition, and a workflow that loads tokens while
      # running it hands those tokens to whatever the PR changed. Uploads
      # happen only from push-to-main and workflow_dispatch, where the code
      # has already been reviewed. Codecov therefore sees mainline history
      # only; the PR-time coverage gate is the floors step above, which
      # needs no secrets.
      - uses: 1password/load-secrets-action@70062d7a876d3eb6334754fa26efd2fbd90c32f2 # v5.0.1
        if: ${{ !cancelled() && github.event_name != 'pull_request' }}
        id: secrets
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          CODECOV_TOKEN: op://Github Actions/CODECOV_TOKEN/credential

      - name: Upload coverage reports to Codecov
        if: ${{ !cancelled() && github.event_name != 'pull_request' }}
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ steps.secrets.outputs.CODECOV_TOKEN }}
          # Both profiles, or the PAM one is collected and thrown away.
          # Codecov merges them; they cover disjoint packages, so there is
          # no double counting.
          files: ./coverage.txt,./coverage-pam.txt
          # `files` adds to what the uploader finds on its own, it does not
          # replace it: without this the search picked up the
          # coverage-combined.txt the floors step leaves in the workspace and
          # uploaded every package a second time -- the double count the
          # comment above that copy exists to prevent.
          disable_search: true
