* hook up acme for http tls
* hitting the login page from a cert request url doesn't show the warning modal and the login page is not the nice one
* host mapping should require a flag, not a config file setting
* should not have service_enrollment_file we print code on screen, if user doesn't save it they lost their code

* approval should let you pick one or more pricipals for user cert, service cert is good with single selection