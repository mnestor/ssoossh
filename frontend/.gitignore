# Frontend-only rules. Paths under this directory can only be matched from
# here, so anything frontend-specific belongs in this file rather than the
# repo root — see the root .gitignore for the anchoring policy that keeps
# source files from being hidden.

node_modules

# Output
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env, templates included: nothing here belongs in the repo.
.env
.env.*

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
